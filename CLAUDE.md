# CLAUDE.md - Project Conventions and Guidelines

## Project Overview
This is a Narrative Modeling App - an AI-guided platform that democratizes machine learning by helping non-expert analysts build, explore, and deploy models without writing code.

## Key Architecture Components

### Frontend (Next.js)
- Located in `apps/frontend/`
- Uses App Router pattern
- TypeScript with strict typing
- Tailwind CSS for styling
- NextAuth v5 for authentication (Google, GitHub providers)

### Backend (FastAPI)
- Located in `apps/backend/`
- FastAPI with async/await patterns
- MongoDB with Beanie ODM
- AWS S3 for file storage
- Background tasks for AI processing

#### AutoML training (issue #75)
- `POST /api/v1/ml/train` runs real AutoML training in a FastAPI `BackgroundTasks` job and creates a `TrainingJob` (`app/models/training_job.py`, registered in `app/models/registry.py`).
- `GET /api/v1/ml/{model_id}/status` returns live progress, the ranked model comparison, rule-based algorithm recommendations, and a plain-language best-model explanation; failures are recorded on the job (status `failed` + `error`) instead of being silently re-raised.
- The engine (`app/services/model_training/automl_engine.py`) applies basic class-imbalance handling (`class_weight="balanced"` when the majority/minority ratio exceeds 2:1) and accepts a `progress_callback`. Result summaries live in `app/services/model_training/comparison.py`.
- **Issue #76 (polling-based progress monitoring):** `GET /api/v1/ml/jobs` (list jobs, status filter + pagination), `GET /api/v1/ml/{model_id}/logs` (level filter + pagination), `POST /api/v1/ml/{model_id}/cancel` (409 on terminal state); `GET /api/v1/ml/{model_id}/status` extended with `current_stage`, `elapsed_seconds`, `estimated_remaining_seconds`, `cancellation_requested`; `TrainingJob` gains `logs` (structured `TrainingLogEntry`), `cancellation_requested`, and stage/timing fields; `AutoMLEngine` supports cooperative cancellation via `cancel_check` callback and optional `event_callback`; frontend has `TrainingProgress`, `TrainingLogs`, `CancelTrainingButton` components and a `/training` dashboard page. WebSocket/SSE and Redis progress caching deliberately not implemented (polling chosen for beta).
- **Out of scope / deferred:** time-series (ARIMA/Prophet), SMOTE resampling, Quick/Comprehensive training modes (#101), hyperparameter tuning (#77).
- **Issue #79 (model evaluation dashboard — backend):** the API contract lives in `app/schemas/evaluation.py` (mirrored by `apps/frontend/lib/types/evaluation.ts` — change both together). Training persists the best model's held-out arrays (`y_test`/`y_pred`/`y_proba`/original-space `class_labels`, captured on `AutoMLResult`) as JSON to S3 at `models/{user_id}/{model_id}/evaluation_data.json` (`build_evaluation_payload` in `app/services/model_storage.py`; upload is best-effort and never fails training; `MLModel.evaluation_data_path` records it, `None` for pre-#79 models). `GET /api/v1/ml/{model_id}/evaluation` computes detailed metrics, confusion matrix, and ROC/PR curves (≤200 points/curve) via `app/services/metrics_service.py`, plus a plain-language AI report card from `app/services/evaluation_explanation_service.py` (OpenAI JSON mode behind the shared circuit breaker, deterministic rule-based fallback when no API key — endpoint works fully without one); models without artifacts degrade to `partial=true` with stored scalar metrics (never 500). `POST /api/v1/ml/compare` compares 2–5 owned models (404 unknown/foreign, 400 mixed dataset or problem type), registered before the `/{model_id}` routes like `GET /jobs`.
- **Issue #79 (frontend):** `app/evaluate/page.tsx` is a full dashboard (Tabs: Overview with metric cards + AI "Model Report Card", Confusion Matrix with cell-click drill-down, ROC/PR Curves, Compare with 2–5 model selector) backed by `ModelService.getEvaluation`/`compareModels`; components `ConfusionMatrixChart` (SVG heatmap, keyboard-accessible), `ROCCurveChart`/`PRCurveChart` (Recharts), `ModelComparisonTable` (direction-aware best-per-metric highlighting via `isLowerBetter`). CSV/PDF export in `lib/utils/export.ts` (jspdf@4 + jspdf-autotable@5, pure `build*` functions for testability). `app/evaluate/[datasetId]/page.tsx` re-exports the page because workflow navigation pushes `/evaluate/{datasetId}`. Demo-found fixes: legacy `/api/v1/upload/` now sets `UserData.file_type` (training requires it). E2E spec `e2e/workflows/evaluate.spec.ts` passes once the e2e storage prerequisites are met (#191, fixed).
- **Issue #191 (e2e upload fixture / test storage):** the "drifted fixture" was actually missing S3-compatible storage — `test-e2e.sh` now auto-detects LocalStack on :4566 (start it via `docker compose -f apps/backend/docker-compose.test.yml up -d localstack`; the LocalStack volume must mount at `/var/lib/localstack`, not the legacy `/tmp/localstack`, which crashes 3.x), and `seed_e2e_data.py` creates `test-bucket` on the configured endpoint (never real AWS). The `uploadTestDataset` fixture fails with step-level diagnostics (step name, URL, visible `upload-error` text); `upload-fixture-smoke.spec.ts` (@smoke) validates the fixture in isolation — run it first when upload UI changes. Upload error states carry `data-testid="upload-error"` / `upload-error-message`. See `apps/frontend/e2e/README.md` → Prerequisites.
- **Issue #87 (backend workflow persistence):** workflow stage/progress state persists to MongoDB per `(user_id, dataset_id)` — `WorkflowState` in `app/models/workflow.py` (unique compound index; embedded `state_history` append log of full snapshots, capped at the latest 50; `stage_data` deep-copied into snapshots), `WorkflowService` (`create/get_by_dataset/update_workflow/get_history`; concurrent-create race → `ConflictError`, note Beanie's `save()` surfaces the unique-index violation as `RevisionIdWasChanged`), routes `POST/GET/PUT /api/v1/workflows/{dataset_id}` + `GET .../history` (wrong user → 404 by user-scoped lookup, duplicate POST → 409, `stage_data` serialized size capped at 256KB → 422). Frontend `WorkflowContext` loads backend state during hydration **before** `isHydrated` flips (dataset id from prop, localStorage cache, or the stage-page URL — direct `/explore/{id}` loads recover after a cleared cache), auto-saves at stage boundaries (completion *and* `currentStage` change), POST→PUT with 409 retry, skips identical-state saves (`lastSavedRef`), and falls back to localStorage when the backend is unreachable. Concurrent PUTs are last-writer-wins (accepted beta limitation; multi-device sync deferred with #91).
- **Issue #82 (prediction interfaces — single + batch):** `POST /api/v1/ml/{model_id}/predict` already did real inference (not a stub — the Traycer plan was stale); it now validates required raw features (clear **422** listing the missing ones), returns backward-compatible per-record `confidence` (max class proba) + `class_labels`, caps `data` at `MAX_PREDICT_RECORDS` (1000), and maps a sklearn `ValueError`/`KeyError` (e.g. unknown category) to a 422 instead of a 500. New `GET /api/v1/ml/{model_id}/features` returns the **raw input schema** for the auto-generated form (numeric vs categorical-with-options, class labels, problem type) derived from the persisted `FeatureEngineer.numeric_features`/`categorical_features` (NOT the engineered `feature_names`), with a `feature_names` fallback when artifacts can't load. The previously-broken `BatchPredictionService` is fixed: correct `load_model(model_id, user_id)` tuple call, **awaited** async `feature_engineer.transform`, real S3 (`upload_file_obj`/`download_file_obj`), cumulative counts, summary stats (`prediction_distribution` + `confidence_stats` in `job.results`/`BatchJobResponse.results`), clean tabular results CSV, and background tasks retained in `self._background_tasks` + observed via `add_done_callback` (`_spawn_processing`); tests use `create_batch_prediction_job(auto_start=False)` for determinism. Same `load_model` bug fixed in `production.py`. **Boolean-column fix:** pandas parses `true`/`false` to `bool` dtype, which fell through both FE feature lists — `FeatureEngineer._coerce_booleans` now casts bool→str at fit *and* transform so such columns are encoded as categoricals (`"True"`/`"False"`) and surfaced in the form. Frontend: `model.ts` adds `getModelFeatures` + batch methods (`createBatchJob`/`getBatchJob`/`getBatchJobProgress`/`downloadBatchResults`/`cancelBatchJob`); `app/predict/page.tsx` is wired to these (auto-generated form + real-time validation; single result shows value/confidence/per-class probs; batch = create→poll progress→summary→download CSV) and **gates its redirect on `isHydrated`** (mirrors evaluate) to avoid a premature bounce to `/upload`; `app/predict/[datasetId]/page.tsx` re-exports the page. AC6 round-trip lives in `tests/integration/test_prediction_roundtrip.py` (real Mongo + LocalStack); E2E `e2e/workflows/predict.spec.ts` drives single + batch through the wired UI (note the shared `PredictPage.predict()` selector matches "Single **Predict**ion" — click `data-testid="make-prediction"` directly).
- **Issue #83 (confidence scoring + per-prediction explainability):** builds on #82. **No SHAP** — P3.3 (#80) is still open, and the AC says "fall back to model-native importance," so SHAP is deferred to #80. Two new stateless services: `ConfidenceService` (`app/services/confidence_service.py` — `confidence_from_proba`, `is_low_confidence` at `DEFAULT_LOW_CONFIDENCE_THRESHOLD=0.7`, `calibrate_classifier` via `CalibratedClassifierCV(FrozenEstimator(...))` (replaces deprecated `cv="prefit"`; sigmoid <1000 cal samples else isotonic; Brier/log-loss score — an **in-sample diagnostic**, measured on the calibration-fit split, so optimistic), `residual_std`/`regression_interval` for ±1.96σ intervals) and `PredictionExplainerService` (`app/services/prediction_explainer_service.py` — per-row `coef·x` for linear (`linear_coefficients`), global `feature_importances_` fallback for trees (`tree_importance`), stored `MLModel.feature_importance` fallback (`stored_importance`); unwraps Calibrated/Frozen/Pipeline wrappers; **never raises** — returns `None` when unexplainable). `PredictionEnricher` (`app/services/prediction_enrichment.py`) composes both for the endpoints. **Training-time calibration** (`AutoMLEngine._calibrate_best_model`): the best classifier is calibrated on the held-out split and **swapped in place** so the deployed model yields calibrated probabilities — done **before** `_capture_evaluation_arrays` so the #79 dashboard describes the deployed model (calibration is fit on that same held-out split — documented beta limitation); `feature_importance` is extracted from the **raw** estimator first (the calibrated wrapper hides `coef_`/`feature_importances_`); regression stores `residual_std`. New optional `MLModel` fields (`is_calibrated`, `calibration_method`, `calibration_score`, `residual_std`) + schemas (`PredictionConfidence`/`FeatureContribution`/`PredictionExplanation` in `app/schemas/model.py`) — all default/optional so pre-#83 models degrade (raw max-proba confidence, `is_calibrated=False`, no intervals). Endpoints: training `predict` + `production.py` predict (previously had **no** confidence) + batch all compute proba whenever `predict_proba` exists (drives confidence + `low_confidence` flags even when full vectors aren't requested), gate `explanations` behind `include_explanations` (default off), and add regression `prediction_intervals`; batch adds per-row `low_confidence`, summary `low_confidence_count`, and `low_confidence`/`prediction_interval`/`explanation` CSV columns (confidence column was already #82). Frontend `model.ts` adds the new response fields + `FeatureContribution`/`PredictionExplanation` types; `app/predict/page.tsx` sends `include_explanations: true` for single predictions and renders a low-confidence warning, regression interval, and a "What drove this prediction" feature-contribution panel, plus a batch low-confidence count. Tests: `tests/test_services/test_confidence_service.py`, `test_prediction_explainer_service.py`, extended `test_batch_prediction.py` + `test_api/test_model_training.py` + the `test_prediction_roundtrip.py` integration round-trip (note: mocks of `MLModel` must set the four new fields or FastAPI's `response_model=MLModel` validation rejects the bare MagicMock).
- **Issue #80 (model interpretability — SHAP + feature importance):** delivers the SHAP layer #83 deferred. The Traycer plan was stale/over-scoped (LIME, matplotlib/plotly images, dependence plots, KernelExplainer, cross-model comparison, a `/models/[id]` page, Redis caching — all post-beta per the roadmap); **scope is SHAP + feature importance only** via `shap>=0.46` (verified on py3.13/numpy2.3/sklearn1.7). New stateless `InterpretabilityService` (`app/services/interpretability_service.py`): `TreeExplainer` for tree/ensemble, `LinearExplainer` for linear, **`None` for unsupported types** (KNN/SVM-rbf → model-native fallback, the roadmap's "fall back if blocked"); `compute_global_shap` (mean |SHAP| per feature, multiclass→mean-abs across classes), `compute_instance_shap`/`compute_instance_shap_batch` (per-row, **tree-only** since `LinearExplainer` needs background unavailable at predict time), `top_drivers_text`, shared module-level `unwrap_estimator`; lazy `import shap`, **never raises**, samples ≤200 rows (`DEFAULT_MAX_SAMPLES`) to keep SHAP <30s. **Training-time precompute** (`AutoMLEngine`): global SHAP computed on the held-out set from the **raw** estimator *before* calibration (calibrated wrapper hides tree/linear internals), carried on `AutoMLResult.shap_global`/`shap_explainer_type`, persisted best-effort to S3 `models/{user}/{model}/shap_data.json` (`build_shap_payload`, `MetricsService.load_shap_artifacts`) — keeps the <30s budget off the request path; a cancel is rechecked after the SHAP step. New optional `MLModel.shap_values_path`/`shap_explainer_type` (pre-#80 models degrade). Endpoints under `/api/v1/ml/` (**not** the AC's `/api/v1/models/` — that's a legacy `ModelConfig` surface; the frontend `ModelService` + every real model route live at `/ml/`): `GET /{model_id}/feature-importance` (ranked native + SHAP importance) and `GET /{model_id}/shap` (mean |SHAP| + plain-language drivers), both **partial (never 500)** for pre-#80/unsupported models, 404 foreign. **Per-prediction SHAP** extends `PredictionExplainerService`: tree models now get per-row `TreeExplainer` SHAP (method **`shap_tree`**) instead of #83's global `tree_importance`; linear keeps `coef·x` (`linear_coefficients`); `PredictionEnricher` + `BatchPredictionService` build the explainer **once per request/chunk** via `compute_instance_shap_batch` (not per row — codex/internal review fix). Schemas `RankedFeature`/`FeatureImportanceResponse`/`ShapSummaryResponse` in `app/schemas/evaluation.py` (mirror `lib/types/evaluation.ts`). Frontend: `ModelService.getShapSummary`, Recharts `ShapSummaryChart` (mean |SHAP| bar + drivers), rendered best-effort on the evaluate-page Overview below native importance (silently omitted when partial); the predict page's "What drove this prediction" panel is now SHAP-powered for tree models. Tests: `tests/test_services/test_interpretability_service.py`, `tests/test_api/test_interpretability.py`, extended automl/storage/explainer/enrichment/batch/roundtrip suites; frontend `__tests__/components/ShapSummaryChart.test.tsx`. **Beta limitations:** no LIME/PDP/dependence plots; SHAP summary is the mean-|SHAP| bar (no beeswarm/force plots); per-instance SHAP is tree-only.
- **Issue #88 (seamless stage transitions — frontend UX):** **frontend-only** — backend persistence (#87) and the `WorkflowBar` progress indicator (AC3) already existed; the Traycer plan's backend steps + a `POST /transition` endpoint were stale/redundant (every `PUT /workflows/{id}` already appends a `state_history` snapshot = transition recording). New `lib/utils/stageValidation.ts`: `validateStageCompletion(stage, data)` (DATA_LOADING needs `datasetId`, FEATURE_ENGINEERING ≥2 features, MODEL_TRAINING needs `modelId`, DEPLOYMENT needs `deploymentId`; others informational), ordering helpers, and **`buildStageUrl` (route-aware)** — only `data_profiling`/`model_evaluation`/`prediction` take a `/{route}/{datasetId}` deep-link path; the rest use the bare route (fixes a latent bug where `completeStage`/`setCurrentStage` pushed `/{route}/{datasetId}` for every stage, 404ing `/prepare/{id}` and mis-routing `/model/{id}` to the model *detail* viewer). `WorkflowContext` gains `goToNextStage`/`goToPreviousStage` (route-aware), `guardMessage`+`requestStageRedirect`+`clearGuardMessage`, and **`completeStage(stage, data?, {autoAdvance?})`** — auto-advance is now **opt-in (default off)**; it was previously dead code (stale-closure `canAccessStage(next)` never saw the just-completed stage). Explicit **`StageNavigation`** (`components/workflow/StageNavigation.tsx`: Back + "Continue to <next>", gated on `completedStages` + `validateStageCompletion`, final-stage Finish) drives transitions on prepare/features/model/evaluate/predict/deploy; the explore profiling page keeps its own one-click "Complete & Continue" (now `{autoAdvance:true}`). **`useStageGuard(stage)`** (`lib/hooks/useStageGuard.ts`) replaces the per-page `canAccessStage → router.push('/upload')` blocks: waits for `isHydrated`, redirects to the **earliest incomplete prerequisite** with a helpful message (single-fire ref), used by all 7 gated pages incl. explore. **`StageGuardBanner`** (in `app/layout.tsx`, below `WorkflowBar`) renders the message (dismiss + 8s auto-dismiss). Demo-found/review fixes: features page must gate its load effect on `ready && state.datasetId` (a `[]`-dep effect fetched `/datasets/undefined/features` once on direct load — codex). Tests: `__tests__/lib/utils/stageValidation.test.ts`, `lib/contexts/WorkflowContext.navigation.test.tsx`, `lib/hooks/useStageGuard.test.tsx`, `components/workflow/StageNavigation.test.tsx` + `StageGuardBanner.test.tsx`; E2E `e2e/workflows/stage-transitions.spec.ts` drives the full upload→predict journey via Continue CTAs with the **ML backend mocked via `page.route`** (deterministic; not in the CI gate). **Beta limitations:** concurrent multi-device sync still last-writer-wins (#91); no backend stage-transition validation (frontend `canAccessStage` is the guard).
- **Issue #151 (enforce API rate limiting — backend):** `APIKey.rate_limit` was modeled but never enforced. New global `RateLimitMiddleware` (`app/middleware/rate_limit.py`) limits **every `/api/v1` route** (skips non-`/api/v1` paths and `OPTIONS` preflight). Identity is resolved cheapest-first: **`X-API-Key` only on the model-serving routes** (`/api/v1/production/v1/models/...`, where `verify_api_key` is the real auth — the sibling `/production/api-keys` CRUD is bearer-authed and deliberately excluded so a key can't swap into its larger budget) → per-key `APIKey.rate_limit` over a 1h window; else session Bearer (`get_current_user_id_optional`, reused — no auth drift) → default per-user budget; else client IP. **`X-Forwarded-For` is ignored unless `RATE_LIMIT_TRUST_FORWARDED_FOR=true`** (else spoofable → fresh bucket per request); enable only behind nginx. Pluggable store (`app/services/rate_limit.py`): `RedisRateLimitStore` (atomic `INCR`+`EXPIRE` fixed-window, async, **fails open** on any Redis error) when `REDIS_URL` is set, else `InMemoryRateLimitStore` (process-local; tests + single-instance fallback). Over-budget → **429 + `Retry-After`**; all `/api/v1` responses carry `X-RateLimit-Limit/Remaining/Reset`. Registered **innermost** (added before CORS) so CORS stays outermost and decorates the 429. Store built in lifespan onto `app.state.rate_limit_store`. New `Settings`: `REDIS_URL`, `RATE_LIMIT_ENABLED`/`_DEFAULT_REQUESTS`/`_DEFAULT_WINDOW_SECONDS`/`_APIKEY_WINDOW_SECONDS`/`_TRUST_FORWARDED_FOR` (all env-configurable; see `.env.example`). Removed the redundant sync-Redis `check_rate_limit` from `production.py`; `APIKey.hash_key` is now the single hashing source. **Disabled by default in the test env** (`tests/conftest.py` sets `RATE_LIMIT_ENABLED=false`) to avoid cross-test bucket bleed — the rate-limit tests opt in with `enabled=True`. Tests: `tests/test_middleware/test_rate_limit.py` (unit, in-memory) + `tests/test_integration/test_rate_limit_integration.py` (real Redis :6380 + per-key override over real Mongo). **Beta limitations:** fixed-window (not sliding/token-bucket); fail-open on Redis outage (availability > strict enforcement); in-memory fallback is per-worker (not shared).
- **Issue #152 (beta launch readiness — onboarding integration, user docs, feedback):** wires the four P4.2 deliverables. **Feedback (backend):** new `Feedback` Beanie doc (`app/models/feedback.py`, registered in `app/models/registry.py`) + `FeedbackRequest`/`FeedbackResponse`/`FeedbackCategory` schemas (`app/schemas/feedback.py`; rating 1–5, category enum bug/feature_request/general/onboarding, message, optional `page_context`); `POST /api/v1/feedback` (`app/api/routes/feedback.py`, registered in `main.py`) is auth-required (`get_current_user_id`), returns **201** with the stored record, **422** on validation, and an **opaque 500** that logs the stack trace only (no `user_id`, no driver/connection-string leak). Tests: `tests/test_api/test_feedback.py` (integration; 201/persistence/validation/auth/opaque-500). **Onboarding integration (frontend):** new-user OAuth now routes through `/dashboard` (not `/onboarding`) so the dashboard's completion check drives the redirect and completed users aren't dead-ended; `lib/hooks/useOnboardingStatus.ts` fetches `GET /api/v1/onboarding/status` and **fails open** (`isComplete=true` on error — never traps a user); `app/dashboard/page.tsx` redirects incomplete users to `/onboarding` unless a **per-user** localStorage skip flag (`onboarding_skipped:<userId>`, set by `?skipOnboarding=true`) is present; `/onboarding` added to `middleware.ts` protected routes. **Quickstart (frontend):** static `app/quickstart/page.tsx` documents all 8 stages (purpose/actions/outcome/route per stage) with anchor nav; broken `/docs` links fixed → `/quickstart` (onboarding completion screen + `OnboardingStep` Help). **Feedback widget (frontend):** `components/FeedbackWidget.tsx` — floating bottom-right widget (star rating, category, message) POSTing to `/api/v1/feedback` with `page_context = location.pathname`, **inline success/error states (no toast dependency)** and a 15s AbortController timeout; mounted in `app/layout.tsx` inside the authenticated block. **E2E (AC4/AC5):** `e2e/workflows/beta-journey.spec.ts` (`@beta-acceptance`) — onboarding routing scenarios, the full journey mapped to the **10 `BETA_ROADMAP.md` criteria**, feedback widget success/error, and a **p95<500ms** API check via new `PerformanceMonitor.measureP95` (measured calls assert `response.ok()` so a fast error can't pass); journey/routing/feedback mock the backend, the perf test hits the live stack and skips if unreachable. Verified: all 7 beta-journey tests pass against the full live stack. Frontend tests: `__tests__/lib/hooks/useOnboardingStatus.test.tsx`, `__tests__/app/quickstart.test.tsx`, `__tests__/components/FeedbackWidget.test.tsx`. **Beta limitations / known gaps:** `useOnboardingStatus` uses the app-wide placeholder auth token (`lib/auth-helpers.ts`) — under `SKIP_AUTH=false` the backend can't decode it, so the status check fails open and the redirect won't fire in production until real JWT exchange lands (app-wide, separate issue); E2E suite is **not** in the CI gate (manual `workflow_dispatch`); skip state is last-writer-wins across devices (#91).
- **Issue #153 (frontend cleanup — CSV export + feature-selection comparison view):** removed the two shipped TODO markers by implementing the features they described (frontend-only; no backend changes — `MethodComparisonResponse` already carried all the data). **CSV export:** `PreviewControls` gains an optional `onExport` callback (mirrors `onRefresh`); the Export button is disabled until a parent supplies it (and while `loading`). `TransformationPreview` builds the CSV from `preview_result.transformed_data` (header = union of row keys, preserving first-seen order) and downloads `transformation-preview-{datasetId}.csv`, reusing the RFC 4180-style field-quoting + Blob/anchor pattern from `SelectedFeatureSet.tsx`; array/object cells are `JSON.stringify`-serialized (never `[object Object]`), and a field is quoted when it contains `,`/`"`/`\n`/`\r`. Line endings are **LF** to match the existing `SelectedFeatureSet` convention (not strict RFC 4180 CRLF — accepted by all common parsers incl. Excel). **Comparison view:** `FeatureSelection` now preserves the full `MethodComparisonResponse` (instead of collapsing to the first method) in new `comparisonResult` state, adds a third **Comparison** tab (disabled until a comparison runs, auto-selected on success; `result`/single-method flow untouched), and renders the new `components/MethodComparisonView.tsx` (side-by-side method cards with selected features + top scores, prominent consensus features, a numeric pairwise overlap matrix, the `recommendations` text, and a CSV export of the comparison). Tests: extended `__tests__/components/transformation/PreviewControls.test.tsx` + `TransformationPreview.test.tsx` (export wiring, disabled states, real CSV content incl. complex-type + CR quoting), new `__tests__/components/MethodComparisonView.test.tsx` + `FeatureSelection.test.tsx` (3-tab structure, comparison state, auto-switch, service mocked). Incidental: removed a pre-existing unused `TransformationService` import in `TransformationPreview.tsx`. CodeRabbit (CLI + PR) findings addressed: JSON-serialize complex CSV cells, quote `\r`, props JSDoc. **Note:** the comparison flow still uses a fixed method set (`correlation`, `mutual_info`, `random_forest`, `statistical`), unchanged.
- **Issue #157 (stabilize flaky performance smoke tests):** the root cause was **wall-clock timing assertions inside the blocking `@smoke` gate** (`smoke-tests.yml`, runs every PR) — on shared 2-core GitHub runners `Date.now()` deltas around page loads / API calls are contention-sensitive, so identical code flaked. Fix is to **split timing tests out of `@smoke` into a new `@perf` tag** (decided over retuning thresholds): in `e2e/workflows/performance.spec.ts` the dashboard-load test is un-`fixme`'d, retagged `@perf`, threshold widened to a CI-realistic **5000ms TTI ceiling**; the single-prediction test is retagged `@perf` with the intended SLO documented as **1000ms** (was a 2000ms stability margin; ~180ms local). The blocking gate loses **no functional coverage** — functional prediction smoke already lives in `predict.spec.ts (@smoke)`. New non-blocking **`.github/workflows/perf-tests.yml`** runs `@perf` on `chromium-full` with **`continue-on-error: true`** (records latencies into the `performance-results` artifact, never blocks the PR; triggers on `performance.spec.ts`/`PerformanceMonitor.ts` changes + `workflow_dispatch`); `npm run test:e2e:perf` = `--grep @perf --project=chromium-full`. The two `transform.spec.ts @smoke` tests stay in `@smoke` (they assert *functional* transformation, not timing) and keep their `test.slow()` (from #156). Removing the two core-pegging perf tests from `@smoke` relieves contention but did not fully stabilize the gate — CI showed the remaining flakes are heavy **train-then-assert** `@smoke` specs (`evaluate.spec.ts`, `model-config.spec.ts`, `data-preparation.spec.ts`, `train.spec.ts`) whose upload/training in `beforeEach` exceeded the 30s test budget (hook/fixture setup counts against it) under 2-core worker contention; each now calls **`test.slow()` in its `beforeEach`** (same #156 pattern) to triple the budget. (`complete-ai-workflow.spec.ts` already had a file-level `test.setTimeout(300000)`.) No `playwright.config.ts` worker-count change, no `PerformanceMonitor`/backend changes. **Note:** `smoke-tests.yml` is a non-required check (the required gate is `ci.yml`'s `CI Success` aggregate) and had been red on every recent PR from this same systemic 2-core starvation.
- **Issue #170 (InteractiveVisualizationDashboard renders real data, not `Math.random()`):** the dashboard generated chart payloads with `Math.random()` (`sampleChartData` memo: scatter/line/histogram) + a static `sampleBoxPlotData`; only correlation was real. **Frontend:** histogram now uses `HistogramChart`'s fetch-by-column mode (`datasetId`/`column`/new `bins` prop); boxplot/scatter/line use real services (`getBoxPlot`/`getScatterPlot`/`getLineChart`) via per-chart local fetch state + `getAuthToken()` + stale-response guards (data cleared on refetch/error so export can't serialize a stale selection); **line charts accept a non-numeric (datetime/text) X axis** — only the Y series must be numeric; explicit empty/unsupported guards per chart (never fabricates data); real-data export (`handleExportChart` serializes the active chart's fetched data; histogram re-fetches on demand); real refetch replaces the simulated `setTimeout`. Removed `sampleChartData`/`sampleBoxPlotData`. **Pre-existing bug fixed:** the dashboard's `Tabs` was hardcoded to `value="configure"`, so the **Visualize** tab (where charts render) could never display — made it controlled (`activeTab`). **Backend contract fixes exposed by wiring real endpoints:** scatter/line/timeseries loaded from `dataset.file_path` (a raw S3 key the downloader rejects → **500 on normal uploads**) — switched to `dataset.s3_url` (always usable; consistent with the histogram/boxplot/correlation cache service); the line route returned a raw `numpy` x → FastAPI 500, now coerced to native int/float/str; `getHistogram` sent `?bins=` but the route param is `num_bins` (custom bin counts were silently ignored) — fixed; the histogram cache key omits `num_bins`, so only the default count is cached and non-default counts compute fresh (a cached default entry is validated against its bin count before serving — self-heals pollution); `getBoxPlot`/`getScatterPlot`/`getLineChart` now `encodeURIComponent` column path segments (handles spaces/`#`/`&`; a literal `/` in a column name still 404s — Starlette decodes the path before routing — pre-existing). **Demo-found fix:** `/user_data/{id}` returns the id as `_id` with `id:null`, so the explore page passed `datasetId={null}` to the dashboard and no chart could fetch — `app/explore/[id]/page.tsx` now normalizes `dataset.id` from `_id` (route-param fallback) on load and when merging processing results. Tests: `__tests__/components/InteractiveVisualizationDashboard.test.tsx` (11 cases), extended `HistogramChart.test.tsx`/`visualization.test.ts`, backend `test_api/test_visualizations.py` (s3_url + numpy-x) + `test_services/test_visualization_cache.py` (bin-count bypass + stale self-heal), explore `page.test.tsx` (id normalization). **Beta limitation:** visualizations load the **original uploaded** dataset (`s3_url`); showing the post-transformation version requires normalizing the `file_path` key/URL inconsistency across upload+transformation **and** a source-aware viz cache — deferred (verified that naively preferring `file_path` 500s every untransformed dataset).
- **Issue #168 (deploy page contract mismatch — frontend):** `app/deploy/page.tsx` spoke a contract the backend never had, so deploying from the UI silently never worked. **Fixes:** `handleDeploy` now uses **`PUT /models/{id}/deploy`** (was POST → wrong route) and reads `deployment_endpoint`/`model_id` (was `api_endpoint`/`api_key`/`id`, none of which `ModelDeployResponse` returns); `completeStage` records `model_id`+`deployment_endpoint`. Because `mark_deployed(endpoint)` **persists the endpoint verbatim and synthesizes no default**, the UI supplies the production serving URL (`${API_URL}/production/v1/models/{id}` — where `production_predict` is registered) as `endpoint`, else the success page would show a `null` endpoint. `checkDeploymentStatus` previously hit a **nonexistent** `GET /models/{id}/deployment` (silent 404 on mount) → now reads `deployment_config.{is_deployed,deployment_endpoint,deployed_at}` from `GET /models/{id}`. Success UI drops the **API Key** section (backend issues none on deploy; keys come from account settings) and the example `curl` now uses the real production contract (`X-API-Key` header, `{ "data": [...] }` body — `ProductionPredictRequest`, `verify_api_key`). `lib/types/api.ts` `DeployResponse` now **mirrors backend `ModelDeployResponse`** (`{model_id,status,deployed_at,deployment_endpoint:string|null,message}`) with a narrow `ModelDeploymentView` for the status check, so future drift fails `tsc`. Tests: `__tests__/app/deploy/page.test.tsx` (PUT+endpoint-in-body, status-check route, real endpoint render, no api-key section, stage completion). **Out of scope:** `lib/services/visualization.ts` `getBoxPlot`/`getCorrelationMatrix` share the same camelCase drift but have **no callers** (`useVisualizations` unused) — deferred (YAGNI).

### MCP Server
- Located in `apps/mcp/`
- FastMCP framework
- Tools for advanced data processing and modeling

## Testing Commands
- Backend (full suite): `cd apps/backend && uv run pytest` — requires MongoDB on localhost:27017; optional test Redis (6380) and LocalStack (4566) via `docker compose -f docker-compose.test.yml up -d` (tests skip with a reason when these are absent)
- Backend (service-free tests, no services): `cd apps/backend && PYTHONPATH=. uv run pytest tests/test_security/ tests/test_processing/ tests/test_utils/ tests/test_models/ tests/test_auth/ tests/test_model_training/test_problem_detector.py tests/test_model_training/test_feature_engineer.py -m "not integration and not performance" -v`
- Frontend (unit tests): `cd apps/frontend && npm test`
- Frontend (type check): `cd apps/frontend && npm run type-check`
- MCP: `cd apps/mcp && uv run pytest tests/` (scope to `tests/`; the vendored `fastmcp/tests/` is the upstream library's own suite)

## Test Suite Status
- Backend: full suite green locally (~1,460 passed, ~39 service-gated/documented skips) ✅ — fixed in issue #160
  - During pytest runs the app lifespan uses the **test** database (never production `MONGODB_URI`)
  - Canonical Beanie model registry: `app/models/registry.py` (shared by app lifespan and `setup_database` fixture)
- CI: `ci.yml` is the primary PR gate (issue #150). It runs backend (ruff **blocking**, mypy **advisory**, service-free pytest), frontend (eslint, `tsc --noEmit`, `next build`, jest), MCP pytest, and backend integration tests (MongoDB **service container** on 27017 + Redis/LocalStack from `docker-compose.test.yml`). A single aggregate `CI Success` status is the required check for branch protection on `main`. The old `unit-tests.yml` was consolidated into `ci.yml`; `integration-tests.yml`/`e2e-tests.yml` remain manual (`workflow_dispatch`). `deploy.yml` deploys `main` to staging over SSH (secret-gated; see workflow header) — service requirements documented in each workflow header. `claude-code-review.yml` posts an **advisory** Claude review on each PR — hardened after the PR #187 runaway incident with a 10-min job timeout, `--max-turns 20` via `claude_args` (the action has no `max_turns` input), a per-PR concurrency group with cancel-in-progress, and a prompt that mandates a single consolidated comment (issue #188); `claude.yml` answers `@claude` mentions (30-min timeout, `--max-turns 50`)
- Frontend: Jest tests configured; TypeScript errors eliminated (#166); `npm run type-check` (`tsc --noEmit`) enforced in CI
- MCP: Pytest suite available
- See `apps/backend/docs/TEST_INFRASTRUCTURE.md` for the testing guide and the **Service Prerequisites** table
- See `apps/backend/docs/TDD_GUIDE.md` for TDD methodology

## Environment Variables
- Frontend: `.env.local`
- Backend: `.env`
- Required: AWS credentials, MongoDB URI, OpenAI API key, NextAuth secret
- Development: Set `SKIP_AUTH=true` to bypass authentication — only honored when `ENVIRONMENT` is explicitly `development`/`test`; backend startup fails hard otherwise (#149)

## Data Flow
1. User uploads file → Backend processes → Stores in S3
2. Metadata saved to MongoDB
3. Background AI analysis triggered
4. Frontend displays results with visualizations

## Current Stage
**Sprint 11 Complete** ✅ | **Sprint 12 Ready** 🟢

See `apps/backend/docs/SPRINTS.md` for detailed sprint history.

## MCP Server Setup
This project includes a custom MCP server for advanced data processing. To use it with Claude Desktop:

```json
{
  "mcpServers": {
    "narrative-modeling": {
      "command": "uv",
      "args": [
        "--directory",
        "/path/to/narrative-modeling-app/apps/mcp",
        "run",
        "mcp",
        "dev",
        "server.py"
      ]
    }
  }
}
```

Add this to `~/.config/claude/claude_desktop_config.json` and restart Claude Desktop. Replace `/path/to/narrative-modeling-app` with your actual project path.

Additional recommended MCP servers:
- **Context7** - For library documentation lookup
- **Serena** - For project memory and session management

## Documentation Requirements

**ALL implementation documentation MUST remain synchronized with the codebase**:

1. **API Documentation**:
   - Update OpenAPI specifications when endpoints change
   - Document all request/response schemas
   - Include example requests and responses
   - Document error responses and status codes

2. **Code Documentation**:
   - Python: Docstrings for all public functions, classes, and modules
   - TypeScript: JSDoc comments for complex functions and components
   - Update inline comments when implementation changes
   - Remove outdated comments immediately

3. **Implementation Documentation**:
   - Update relevant sections in this CLAUDE.md file
   - Keep architecture diagrams current
   - Update configuration examples when defaults change
   - Document breaking changes prominently

4. **README Updates**:
   - Keep feature lists current
   - Update setup instructions when dependencies change
   - Maintain accurate command examples
   - Update version compatibility information

5. **CLAUDE.md Maintenance**:
   - Add new patterns to relevant sections
   - Update "Current Stage" when workflow changes
   - Keep command examples accurate and tested
   - Document new testing patterns or quality gates

## Automated Workflow Configuration

### Traycer AI Integration

When receiving Traycer AI prompts:
1. Save prompt to: `prompts/<issue-id>.txt`
2. Run: `./scripts/traycer-workflow.sh <issue-id>`
3. Monitor: `npx claude-flow@alpha status --watch`
4. Only intervene if blocker queue triggered

### Quality Gates

Before PR creation:
- ✅ All tests must pass (100% requirement)
- ✅ Test coverage >85%
- ✅ Linting (ruff/eslint) passes
- ✅ Type checking (mypy/tsc) passes
- ✅ No TODO/FIXME/NotImplemented markers
- ✅ Security scan (OWASP patterns)

### CodeRabbit Configuration

Max iterations: 3
Auto-fix categories:
- Code style and formatting
- Type errors
- Simple logic bugs
- Documentation improvements

Blocker queue triggers:
- Iteration 3 still has failing tests
- Architecture change suggestions
- Security vulnerabilities requiring human decision
