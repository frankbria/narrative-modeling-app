# CLAUDE.md - Project Conventions and Guidelines

## Project Overview
This is a Narrative Modeling App - an AI-guided platform that democratizes machine learning by helping non-expert analysts build, explore, and deploy models without writing code.

## Key Architecture Components

### Frontend (Next.js)
- Located in `apps/frontend/`
- Uses App Router pattern
- TypeScript with strict typing
- Tailwind CSS for styling
- NextAuth v5 for authentication (Google, GitHub providers)

### Backend (FastAPI)
- Located in `apps/backend/`
- FastAPI with async/await patterns
- MongoDB with Beanie ODM
- AWS S3 for file storage
- Background tasks for AI processing

#### AutoML training (issue #75)
- `POST /api/v1/ml/train` runs real AutoML training in a FastAPI `BackgroundTasks` job and creates a `TrainingJob` (`app/models/training_job.py`, registered in `app/models/registry.py`).
- `GET /api/v1/ml/{model_id}/status` returns live progress, the ranked model comparison, rule-based algorithm recommendations, and a plain-language best-model explanation; failures are recorded on the job (status `failed` + `error`) instead of being silently re-raised.
- The engine (`app/services/model_training/automl_engine.py`) applies basic class-imbalance handling (`class_weight="balanced"` when the majority/minority ratio exceeds 2:1) and accepts a `progress_callback`. Result summaries live in `app/services/model_training/comparison.py`.
- **Issue #76 (polling-based progress monitoring):** `GET /api/v1/ml/jobs` (list jobs, status filter + pagination), `GET /api/v1/ml/{model_id}/logs` (level filter + pagination), `POST /api/v1/ml/{model_id}/cancel` (409 on terminal state); `GET /api/v1/ml/{model_id}/status` extended with `current_stage`, `elapsed_seconds`, `estimated_remaining_seconds`, `cancellation_requested`; `TrainingJob` gains `logs` (structured `TrainingLogEntry`), `cancellation_requested`, and stage/timing fields; `AutoMLEngine` supports cooperative cancellation via `cancel_check` callback and optional `event_callback`; frontend has `TrainingProgress`, `TrainingLogs`, `CancelTrainingButton` components and a `/training` dashboard page. WebSocket/SSE and Redis progress caching deliberately not implemented (polling chosen for beta).
- **Out of scope / deferred:** time-series (ARIMA/Prophet), SMOTE resampling. (Quick/Comprehensive training modes shipped in #101; hyperparameter tuning in #77.)
- **Issue #79 (model evaluation dashboard — backend):** the API contract lives in `app/schemas/evaluation.py` (mirrored by `apps/frontend/lib/types/evaluation.ts` — change both together). Training persists the best model's held-out arrays (`y_test`/`y_pred`/`y_proba`/original-space `class_labels`, captured on `AutoMLResult`) as JSON to S3 at `models/{user_id}/{model_id}/evaluation_data.json` (`build_evaluation_payload` in `app/services/model_storage.py`; upload is best-effort and never fails training; `MLModel.evaluation_data_path` records it, `None` for pre-#79 models). `GET /api/v1/ml/{model_id}/evaluation` computes detailed metrics, confusion matrix, and ROC/PR curves (≤200 points/curve) via `app/services/metrics_service.py`, plus a plain-language AI report card from `app/services/evaluation_explanation_service.py` (OpenAI JSON mode behind the shared circuit breaker, deterministic rule-based fallback when no API key — endpoint works fully without one); models without artifacts degrade to `partial=true` with stored scalar metrics (never 500). `POST /api/v1/ml/compare` compares 2–5 owned models (404 unknown/foreign, 400 mixed dataset or problem type), registered before the `/{model_id}` routes like `GET /jobs`.
- **Issue #79 (frontend):** `app/evaluate/page.tsx` is a full dashboard (Tabs: Overview with metric cards + AI "Model Report Card", Confusion Matrix with cell-click drill-down, ROC/PR Curves, Compare with 2–5 model selector) backed by `ModelService.getEvaluation`/`compareModels`; components `ConfusionMatrixChart` (SVG heatmap, keyboard-accessible), `ROCCurveChart`/`PRCurveChart` (Recharts), `ModelComparisonTable` (direction-aware best-per-metric highlighting via `isLowerBetter`). CSV/PDF export in `lib/utils/export.ts` (jspdf@4 + jspdf-autotable@5, pure `build*` functions for testability). `app/evaluate/[datasetId]/page.tsx` re-exports the page because workflow navigation pushes `/evaluate/{datasetId}`. Demo-found fixes: legacy `/api/v1/upload/` now sets `UserData.file_type` (training requires it). E2E spec `e2e/workflows/evaluate.spec.ts` passes once the e2e storage prerequisites are met (#191, fixed).
- **Issue #191 (e2e upload fixture / test storage):** the "drifted fixture" was actually missing S3-compatible storage — `test-e2e.sh` now auto-detects LocalStack on :4566 (start it via `docker compose -f apps/backend/docker-compose.test.yml up -d localstack`; the LocalStack volume must mount at `/var/lib/localstack`, not the legacy `/tmp/localstack`, which crashes 3.x), and `seed_e2e_data.py` creates `test-bucket` on the configured endpoint (never real AWS). The `uploadTestDataset` fixture fails with step-level diagnostics (step name, URL, visible `upload-error` text); `upload-fixture-smoke.spec.ts` (@smoke) validates the fixture in isolation — run it first when upload UI changes. Upload error states carry `data-testid="upload-error"` / `upload-error-message`. See `apps/frontend/e2e/README.md` → Prerequisites.
- **Issue #87 (backend workflow persistence):** workflow stage/progress state persists to MongoDB per `(user_id, dataset_id)` — `WorkflowState` in `app/models/workflow.py` (unique compound index; embedded `state_history` append log of full snapshots, capped at the latest 50; `stage_data` deep-copied into snapshots), `WorkflowService` (`create/get_by_dataset/update_workflow/get_history`; concurrent-create race → `ConflictError`, note Beanie's `save()` surfaces the unique-index violation as `RevisionIdWasChanged`), routes `POST/GET/PUT /api/v1/workflows/{dataset_id}` + `GET .../history` (wrong user → 404 by user-scoped lookup, duplicate POST → 409, `stage_data` serialized size capped at 256KB → 422). Frontend `WorkflowContext` loads backend state during hydration **before** `isHydrated` flips (dataset id from prop, localStorage cache, or the stage-page URL — direct `/explore/{id}` loads recover after a cleared cache), auto-saves at stage boundaries (completion *and* `currentStage` change), POST→PUT with 409 retry, skips identical-state saves (`lastSavedRef`), and falls back to localStorage when the backend is unreachable. Concurrent PUTs are last-writer-wins (accepted beta limitation; multi-device sync deferred with #91).
- **Issue #82 (prediction interfaces — single + batch):** `POST /api/v1/ml/{model_id}/predict` already did real inference (not a stub — the Traycer plan was stale); it now validates required raw features (clear **422** listing the missing ones), returns backward-compatible per-record `confidence` (max class proba) + `class_labels`, caps `data` at `MAX_PREDICT_RECORDS` (1000), and maps a sklearn `ValueError`/`KeyError` (e.g. unknown category) to a 422 instead of a 500. New `GET /api/v1/ml/{model_id}/features` returns the **raw input schema** for the auto-generated form (numeric vs categorical-with-options, class labels, problem type) derived from the persisted `FeatureEngineer.numeric_features`/`categorical_features` (NOT the engineered `feature_names`), with a `feature_names` fallback when artifacts can't load. The previously-broken `BatchPredictionService` is fixed: correct `load_model(model_id, user_id)` tuple call, **awaited** async `feature_engineer.transform`, real S3 (`upload_file_obj`/`download_file_obj`), cumulative counts, summary stats (`prediction_distribution` + `confidence_stats` in `job.results`/`BatchJobResponse.results`), clean tabular results CSV, and background tasks retained in `self._background_tasks` + observed via `add_done_callback` (`_spawn_processing`); tests use `create_batch_prediction_job(auto_start=False)` for determinism. Same `load_model` bug fixed in `production.py`. **Boolean-column fix:** pandas parses `true`/`false` to `bool` dtype, which fell through both FE feature lists — `FeatureEngineer._coerce_booleans` now casts bool→str at fit *and* transform so such columns are encoded as categoricals (`"True"`/`"False"`) and surfaced in the form. Frontend: `model.ts` adds `getModelFeatures` + batch methods (`createBatchJob`/`getBatchJob`/`getBatchJobProgress`/`downloadBatchResults`/`cancelBatchJob`); `app/predict/page.tsx` is wired to these (auto-generated form + real-time validation; single result shows value/confidence/per-class probs; batch = create→poll progress→summary→download CSV) and **gates its redirect on `isHydrated`** (mirrors evaluate) to avoid a premature bounce to `/upload`; `app/predict/[datasetId]/page.tsx` re-exports the page. AC6 round-trip lives in `tests/integration/test_prediction_roundtrip.py` (real Mongo + LocalStack); E2E `e2e/workflows/predict.spec.ts` drives single + batch through the wired UI (note the shared `PredictPage.predict()` selector matches "Single **Predict**ion" — click `data-testid="make-prediction"` directly).
- **Issue #83 (confidence scoring + per-prediction explainability):** builds on #82. **No SHAP** — P3.3 (#80) is still open, and the AC says "fall back to model-native importance," so SHAP is deferred to #80. Two new stateless services: `ConfidenceService` (`app/services/confidence_service.py` — `confidence_from_proba`, `is_low_confidence` at `DEFAULT_LOW_CONFIDENCE_THRESHOLD=0.7`, `calibrate_classifier` via `CalibratedClassifierCV(FrozenEstimator(...))` (replaces deprecated `cv="prefit"`; sigmoid <1000 cal samples else isotonic; Brier/log-loss score — an **in-sample diagnostic**, measured on the calibration-fit split, so optimistic), `residual_std`/`regression_interval` for ±1.96σ intervals) and `PredictionExplainerService` (`app/services/prediction_explainer_service.py` — per-row `coef·x` for linear (`linear_coefficients`), global `feature_importances_` fallback for trees (`tree_importance`), stored `MLModel.feature_importance` fallback (`stored_importance`); unwraps Calibrated/Frozen/Pipeline wrappers; **never raises** — returns `None` when unexplainable). `PredictionEnricher` (`app/services/prediction_enrichment.py`) composes both for the endpoints. **Training-time calibration** (`AutoMLEngine._calibrate_best_model`): the best classifier is calibrated on the held-out split and **swapped in place** so the deployed model yields calibrated probabilities — done **before** `_capture_evaluation_arrays` so the #79 dashboard describes the deployed model (calibration was originally fit on that same held-out split — a documented beta limitation **resolved in #201**, which carves a disjoint calibration slice from the train set so the test split stays a clean holdout); `feature_importance` is extracted from the **raw** estimator first (the calibrated wrapper hides `coef_`/`feature_importances_`); regression stores `residual_std`. New optional `MLModel` fields (`is_calibrated`, `calibration_method`, `calibration_score`, `residual_std`) + schemas (`PredictionConfidence`/`FeatureContribution`/`PredictionExplanation` in `app/schemas/model.py`) — all default/optional so pre-#83 models degrade (raw max-proba confidence, `is_calibrated=False`, no intervals). Endpoints: training `predict` + `production.py` predict (previously had **no** confidence) + batch all compute proba whenever `predict_proba` exists (drives confidence + `low_confidence` flags even when full vectors aren't requested), gate `explanations` behind `include_explanations` (default off), and add regression `prediction_intervals`; batch adds per-row `low_confidence`, summary `low_confidence_count`, and `low_confidence`/`prediction_interval`/`explanation` CSV columns (confidence column was already #82). Frontend `model.ts` adds the new response fields + `FeatureContribution`/`PredictionExplanation` types; `app/predict/page.tsx` sends `include_explanations: true` for single predictions and renders a low-confidence warning, regression interval, and a "What drove this prediction" feature-contribution panel, plus a batch low-confidence count. Tests: `tests/test_services/test_confidence_service.py`, `test_prediction_explainer_service.py`, extended `test_batch_prediction.py` + `test_api/test_model_training.py` + the `test_prediction_roundtrip.py` integration round-trip (note: mocks of `MLModel` must set the four new fields or FastAPI's `response_model=MLModel` validation rejects the bare MagicMock).
- **Issue #80 (model interpretability — SHAP + feature importance):** delivers the SHAP layer #83 deferred. The Traycer plan was stale/over-scoped (LIME, matplotlib/plotly images, dependence plots, KernelExplainer, cross-model comparison, a `/models/[id]` page, Redis caching — all post-beta per the roadmap); **scope is SHAP + feature importance only** via `shap>=0.46` (verified on py3.13/numpy2.3/sklearn1.7). New stateless `InterpretabilityService` (`app/services/interpretability_service.py`): `TreeExplainer` for tree/ensemble, `LinearExplainer` for linear, **`None` for unsupported types** (KNN/SVM-rbf → model-native fallback, the roadmap's "fall back if blocked"); `compute_global_shap` (mean |SHAP| per feature, multiclass→mean-abs across classes), `compute_instance_shap`/`compute_instance_shap_batch` (per-row, **tree-only** since `LinearExplainer` needs background unavailable at predict time), `top_drivers_text`, shared module-level `unwrap_estimator`; lazy `import shap`, **never raises**, samples ≤200 rows (`DEFAULT_MAX_SAMPLES`) to keep SHAP <30s. **Training-time precompute** (`AutoMLEngine`): global SHAP computed on the held-out set from the **raw** estimator *before* calibration (calibrated wrapper hides tree/linear internals), carried on `AutoMLResult.shap_global`/`shap_explainer_type`, persisted best-effort to S3 `models/{user}/{model}/shap_data.json` (`build_shap_payload`, `MetricsService.load_shap_artifacts`) — keeps the <30s budget off the request path; a cancel is rechecked after the SHAP step. New optional `MLModel.shap_values_path`/`shap_explainer_type` (pre-#80 models degrade). Endpoints under `/api/v1/ml/` (**not** the AC's `/api/v1/models/` — that's a legacy `ModelConfig` surface; the frontend `ModelService` + every real model route live at `/ml/`): `GET /{model_id}/feature-importance` (ranked native + SHAP importance) and `GET /{model_id}/shap` (mean |SHAP| + plain-language drivers), both **partial (never 500)** for pre-#80/unsupported models, 404 foreign. **Per-prediction SHAP** extends `PredictionExplainerService`: tree models now get per-row `TreeExplainer` SHAP (method **`shap_tree`**) instead of #83's global `tree_importance`; linear keeps `coef·x` (`linear_coefficients`); `PredictionEnricher` + `BatchPredictionService` build the explainer **once per request/chunk** via `compute_instance_shap_batch` (not per row — codex/internal review fix). Schemas `RankedFeature`/`FeatureImportanceResponse`/`ShapSummaryResponse` in `app/schemas/evaluation.py` (mirror `lib/types/evaluation.ts`). Frontend: `ModelService.getShapSummary`, Recharts `ShapSummaryChart` (mean |SHAP| bar + drivers), rendered best-effort on the evaluate-page Overview below native importance (silently omitted when partial); the predict page's "What drove this prediction" panel is now SHAP-powered for tree models. Tests: `tests/test_services/test_interpretability_service.py`, `tests/test_api/test_interpretability.py`, extended automl/storage/explainer/enrichment/batch/roundtrip suites; frontend `__tests__/components/ShapSummaryChart.test.tsx`. **Beta limitations:** no LIME/PDP/dependence plots; SHAP summary is the mean-|SHAP| bar (no beeswarm/force plots); per-instance SHAP is tree-only.
- **Issue #204 (track shap/numba dependency weight — Docker hardening, follow-up to #80):** measured + made the SHAP stack a removable lever. **Measurement:** `shap` is the *sole* root pulling `numba`+`llvmlite`+`cloudpickle`+`slicer` — ~**57 MB** of wheels (llvmlite's cp313 manylinux2014_x86_64 wheel alone is 53.7 MB) / ~**186 MB** on-disk (llvmlite 162M, numba 21M, shap 3.2M). **Wheels confirmed** for the deploy target (`python:3.13-slim`, glibc, x86_64): cp313 manylinux wheels for llvmlite 0.45.1 + numba 0.62.1 resolve from `uv.lock` — no source build. **Optional-group lever:** shap moved from `[project.dependencies]` to a PEP 735 `[dependency-groups] interpretability` group, kept in `[tool.uv] default-groups = ["dev", "interpretability"]` so local/CI/`uv sync --no-dev` (the Docker build) all still ship SHAP — **beta behaviour unchanged**. A slim production image builds with `uv sync --frozen --no-dev --no-group interpretability` (drops the 5-package stack); the app degrades to model-native importance (`InterpretabilityService → None`, endpoints `partial`) and the Dockerfile `import app.main` guard stays valid because all three `import shap` sites are lazy (inside methods). Dockerfile comment documents the flag; no build change. Verified via `uv sync --dry-run` (default keeps shap; `--no-group` removes exactly shap+numba+llvmlite+cloudpickle+slicer) and `test_interpretability_service.py` (23 passed).
- **Issue #77 (hyperparameter tuning — post-beta V2, backend):** optional automated hyperparameter optimization added to the AutoML pipeline; **opt-in, default off**. The Traycer plan's 14 steps were collapsed to a lean backend-only change (dropped a duplicate `DataProfile` class, a separate `tuning_visualization.py`/`tuning_plots.py` pair, a standalone `POST /models/tune`, request-schema fields, and a standalone doc — see `tasks/todo.md`). New stateless `HyperparameterTuner` (`app/services/model_training/hyperparameter_tuner.py`): three strategies — `grid`/`random` (scikit-learn `GridSearchCV`/`RandomizedSearchCV`) and `bayesian` (optuna TPE + `MedianPruner` per-fold pruning + `timeout`); algorithm-specific, **data-driven** search spaces (`get_search_space` adapts ranges by `n_samples`/`n_features` — large data shrinks tree size/depth) keyed off `_canonical_algorithm` (maps engine candidate names incl. regressor variants; LogReg tunes only `C`, SVM only `C`/`gamma` — penalty/solver & kernel coupling deliberately not tuned to avoid invalid-combo fit errors); `TuningConfig`/`TrialResult`/`TuningResult` dataclasses with **inline visualization data** (`parameter_importance` (optuna-only), `optimization_history`, `improvement_over_default`); `tune()` is **sync** (engine runs it via `asyncio.to_thread`), **never raises** (returns `None` → keep defaults), and Bayesian **degrades to random search** when optuna is absent. `AutoMLEngine` gains `enable_tuning`/`tuning_config`; `_tune_candidates` runs before the training loop, **applies tuned params only when `improvement_over_default > 0`** (no-regression guard — records every search with an `applied` flag for the viz), honors cancellation between candidates **and** mid-Bayesian via a background `_poll_cancellation` task that sets a `threading.Event` the optuna study stops on. `AutoMLResult` gains `tuning_results`/`tuning_strategy`/`improvement_from_tuning` (`_applied_improvement`: applied gain, `0.0` when tuned-but-not-applied, `None` when off). New **optional** `MLModel` fields (`tuning_strategy`/`tuning_time` (sum of per-algo search times, **not** full AutoML wall time)/`improvement_from_tuning`/`tuning_results`) — pre-#77 models degrade. Enabled via the existing `training_config` dict (**no request-schema change**): `{"enable_tuning": true, "tuning_strategy": "bayesian", "tuning_config": {"n_trials": 30}}`. New `GET /api/v1/ml/{model_id}/tuning-results` (registered before `/{model_id}`) returns the viz payload, **partial (never 500)** for untuned/pre-#77 models. **optuna** lives in a PEP 735 `[dependency-groups] tuning` group in `default-groups` (same removable-lever pattern as #204's shap; lazy-imported — a slim `--no-group tuning` image still trains, grid/random only). Tests: `tests/test_model_training/test_hyperparameter_tuner.py` (tuner unit + engine integration + cancellation/applied-flag), `tests/test_api/test_hyperparameter_tuning.py` (config wiring + endpoint degradation). **Beta limitations:** `time_budget` hard-enforced only for Bayesian (grid/random honor `n_trials`+`n_jobs`, budget best-effort); a single grid/random sklearn search can't be interrupted mid-call; tuning runs before calibration/SHAP (those still run on the tuned best model); per-instance frontend charts deferred (endpoint returns the data).
- **Issue #78 (model versioning & history tracking — post-beta V2):** built on the **real `MLModel`** (`/api/v1/ml/`), **not** new documents (Traycer's stale plan) or the dead legacy `ModelConfig`/`/models/` surface (CodeRabbit's plan — never created by the training flow). A **version family** = all `MLModel`s sharing `(user_id, dataset_id, name)`, ordered oldest→newest = version numbers; zero migration, works on existing data, no retrain wiring. New **optional** `MLModel` fields (pre-#78 models degrade to standalone v1, no production flag): `parent_model_id`, `is_production` (bool), `promoted_at`, `environment_metadata`, `dataset_version_id`, `version_notes` + a `(user_id,dataset_id,name)` index. New stateless `ModelVersioningService` (`app/services/model_versioning_service.py`, singleton `model_versioning_service`): `list_family` (NotFoundError on unknown/foreign anchor), `promote_to_production` (sets `is_production` on target, demotes siblings, stamps `promoted_at`; **rollback == promoting an older version** — one op, no separate endpoint), `resolve_parent` (latest family member, chains lineage at training time), `get_production_version`, and module-level `capture_environment()` (python/platform + sklearn/xgboost/lightgbm/numpy versions). Endpoints under `/ml/` registered **before** the catch-all `/{model_id}`: `GET /ml/{model_id}/versions` (browser data: version#, status, scores, dataset version, features, env — 404 unknown/foreign) and `POST /ml/{model_id}/promote` (promote/rollback). **Comparison reuses the existing `POST /ml/compare` (#79)** — no new compare logic. `model_storage.save_model` now best-effort captures `environment_metadata` + `parent_model_id` (via `resolve_parent`, before insert) + optional `dataset_version_id`/`version_notes` from `model_metadata` (never fails the save). Schemas `ModelVersionEntry`/`ModelVersionListResponse`/`PromoteVersionResponse` in `app/schemas/evaluation.py` (mirror `lib/types/evaluation.ts`). Frontend: `ModelService.getModelVersions`/`promoteModelVersion`; new `components/ModelVersions.tsx` (version table + production/active badges, per-row dataset-version + feature-count lineage, promote/rollback buttons, 2+-select side-by-side comparison reusing `ModelComparisonTable`) rendered in a new **"Versions" tab** on `app/model/[id]/page.tsx`. **Note:** the `sample_ml_model` mock fixture had to gain the 6 new fields (recurring `response_model=MLModel` MagicMock-validation gotcha). Tests: `tests/test_api/test_model_versions.py` (real-Mongo family/promote/rollback/404), `tests/test_services/test_model_versioning_service.py` (lineage + env capture), `__tests__/components/ModelVersions.test.tsx`. **Beta limitations / skipped vs Traycer:** no separate ModelVersion/ModelLineage documents, no React Flow graph (lineage = per-row dataset-version + features), no archive/retention/delete endpoint, no audit log; lineage chains within a name-family only (cross-name retrains aren't auto-linked).
- **Issue #81 (error analysis tools — post-beta V2):** built on the **real `MLModel`/`/api/v1/ml/`** surface (the Traycer/CodeRabbit plans targeted the dead legacy `ModelConfig`/`/models/`). **Reuses #79's `evaluation_data.json`** — `build_evaluation_payload` now also persists the held-out **transformed feature matrix** (`X_test` + `feature_names`, both optional → pre-#81 models degrade); `AutoMLResult.x_test` carries it from `X_test_transformed`. No new MLModel field, no new S3 file, no new loader (reuses `MetricsService.load_evaluation_artifacts`), no retrain wiring. New stateless `ErrorAnalysisService` (`app/services/error_analysis_service.py`, singleton, **never raises** — each section guarded by `_safe`): error distribution (overall + per-class rate), confusion pairs (off-diagonal counts), high-error segments (per-feature quantile binning), error clusters (KMeans on scaled error rows, labeled by distinguishing features), error patterns (shallow `DecisionTreeClassifier` surrogate on the error indicator → readable rules; **note sklearn `tree_.value` holds per-class proportions not counts — use `n_node_samples`**), error cases (round-robin across confusion-pair groups so every ranked pair keeps drill-down examples even past the 200 cap), and AI suggestions (OpenAI behind the shared circuit breaker + deterministic rule-based fallback, mirroring `EvaluationExplanationService`; **the OpenAI `to_thread(client...create)` call needs `cast(Any, self.client)` or mypy resolves the overload against literal kwargs**). Regression: "error" = residual beyond 1σ (`std==0` with nonzero bias → all rows are errors; all-zero → none — codex fix). Endpoint `GET /api/v1/ml/{model_id}/errors` (registered before catch-all `/{model_id}`): 404 foreign, `partial=true` for pre-#81/missing-matrix models (distribution/pairs/cases only), never 500. Schemas in `app/schemas/error_analysis.py` (mirror `lib/types/evaluation.ts`). Frontend: `ModelService.getErrorAnalysis`, `components/ErrorAnalysisDashboard.tsx` (overview cards, per-class bars, clickable confusion pairs that **client-side filter** the case table, segments/patterns/clusters sections, AI suggestions) in a new **"Errors" tab** on `app/model/[id]/page.tsx`. Tests: `tests/test_services/test_error_analysis_service.py`, `tests/test_api/test_error_analysis.py` (full/partial/404/regression), extended `test_model_storage_evaluation.py`, `__tests__/components/ErrorAnalysisDashboard.test.tsx` + extended `model.test.ts`. **Beta limitations / skipped vs Traycer:** engineered-feature space (no original-space reconstruction, same as SHAP #80); KMeans + quantile bins (no DBSCAN/t-SNE/PCA scatter); drill-down is client-side (no server-side `POST /error-patterns`); no bespoke viz components (PatternTree/ClusterScatter/SegmentChart) or dedicated route.
- **Issue #88 (seamless stage transitions — frontend UX):** **frontend-only** — backend persistence (#87) and the `WorkflowBar` progress indicator (AC3) already existed; the Traycer plan's backend steps + a `POST /transition` endpoint were stale/redundant (every `PUT /workflows/{id}` already appends a `state_history` snapshot = transition recording). New `lib/utils/stageValidation.ts`: `validateStageCompletion(stage, data)` (DATA_LOADING needs `datasetId`, FEATURE_ENGINEERING ≥2 features, MODEL_TRAINING needs `modelId`, DEPLOYMENT needs `deploymentId`; others informational), ordering helpers, and **`buildStageUrl` (route-aware)** — only `data_profiling`/`model_evaluation`/`prediction` take a `/{route}/{datasetId}` deep-link path; the rest use the bare route (fixes a latent bug where `completeStage`/`setCurrentStage` pushed `/{route}/{datasetId}` for every stage, 404ing `/prepare/{id}` and mis-routing `/model/{id}` to the model *detail* viewer). `WorkflowContext` gains `goToNextStage`/`goToPreviousStage` (route-aware), `guardMessage`+`requestStageRedirect`+`clearGuardMessage`, and **`completeStage(stage, data?, {autoAdvance?})`** — auto-advance is now **opt-in (default off)**; it was previously dead code (stale-closure `canAccessStage(next)` never saw the just-completed stage). Explicit **`StageNavigation`** (`components/workflow/StageNavigation.tsx`: Back + "Continue to <next>", gated on `completedStages` + `validateStageCompletion`, final-stage Finish) drives transitions on prepare/features/model/evaluate/predict/deploy; the explore profiling page keeps its own one-click "Complete & Continue" (now `{autoAdvance:true}`). **`useStageGuard(stage)`** (`lib/hooks/useStageGuard.ts`) replaces the per-page `canAccessStage → router.push('/upload')` blocks: waits for `isHydrated`, redirects to the **earliest incomplete prerequisite** with a helpful message (single-fire ref), used by all 7 gated pages incl. explore. **`StageGuardBanner`** (in `app/layout.tsx`, below `WorkflowBar`) renders the message (dismiss + 8s auto-dismiss). Demo-found/review fixes: features page must gate its load effect on `ready && state.datasetId` (a `[]`-dep effect fetched `/datasets/undefined/features` once on direct load — codex). Tests: `__tests__/lib/utils/stageValidation.test.ts`, `lib/contexts/WorkflowContext.navigation.test.tsx`, `lib/hooks/useStageGuard.test.tsx`, `components/workflow/StageNavigation.test.tsx` + `StageGuardBanner.test.tsx`; E2E `e2e/workflows/stage-transitions.spec.ts` drives the full upload→predict journey via Continue CTAs with the **ML backend mocked via `page.route`** (deterministic; not in the CI gate). **Beta limitations:** concurrent multi-device sync still last-writer-wins (#91); no backend stage-transition validation (frontend `canAccessStage` is the guard).
- **Issue #151 (enforce API rate limiting — backend):** `APIKey.rate_limit` was modeled but never enforced. New global `RateLimitMiddleware` (`app/middleware/rate_limit.py`) limits **every `/api/v1` route** (skips non-`/api/v1` paths and `OPTIONS` preflight). Identity is resolved cheapest-first: **`X-API-Key` only on the model-serving routes** (`/api/v1/production/v1/models/...`, where `verify_api_key` is the real auth — the sibling `/production/api-keys` CRUD is bearer-authed and deliberately excluded so a key can't swap into its larger budget) → per-key `APIKey.rate_limit` over a 1h window; else session Bearer (`get_current_user_id_optional`, reused — no auth drift) → default per-user budget; else client IP. **`X-Forwarded-For` is ignored unless `RATE_LIMIT_TRUST_FORWARDED_FOR=true`** (else spoofable → fresh bucket per request); enable only behind nginx. Pluggable store (`app/services/rate_limit.py`): `RedisRateLimitStore` (atomic `INCR`+`EXPIRE` fixed-window, async, **fails open** on any Redis error) when `REDIS_URL` is set, else `InMemoryRateLimitStore` (process-local; tests + single-instance fallback). Over-budget → **429 + `Retry-After`**; all `/api/v1` responses carry `X-RateLimit-Limit/Remaining/Reset`. Registered **innermost** (added before CORS) so CORS stays outermost and decorates the 429. Store built in lifespan onto `app.state.rate_limit_store`. New `Settings`: `REDIS_URL`, `RATE_LIMIT_ENABLED`/`_DEFAULT_REQUESTS`/`_DEFAULT_WINDOW_SECONDS`/`_APIKEY_WINDOW_SECONDS`/`_TRUST_FORWARDED_FOR` (all env-configurable; see `.env.example`). Removed the redundant sync-Redis `check_rate_limit` from `production.py`; `APIKey.hash_key` is now the single hashing source. **Disabled by default in the test env** (`tests/conftest.py` sets `RATE_LIMIT_ENABLED=false`) to avoid cross-test bucket bleed — the rate-limit tests opt in with `enabled=True`. Tests: `tests/test_middleware/test_rate_limit.py` (unit, in-memory) + `tests/test_integration/test_rate_limit_integration.py` (real Redis :6380 + per-key override over real Mongo). **Beta limitations:** fixed-window (not sliding/token-bucket); fail-open on Redis outage (availability > strict enforcement); in-memory fallback is per-worker (not shared).
- **Issue #152 (beta launch readiness — onboarding integration, user docs, feedback):** wires the four P4.2 deliverables. **Feedback (backend):** new `Feedback` Beanie doc (`app/models/feedback.py`, registered in `app/models/registry.py`) + `FeedbackRequest`/`FeedbackResponse`/`FeedbackCategory` schemas (`app/schemas/feedback.py`; rating 1–5, category enum bug/feature_request/general/onboarding, message, optional `page_context`); `POST /api/v1/feedback` (`app/api/routes/feedback.py`, registered in `main.py`) is auth-required (`get_current_user_id`), returns **201** with the stored record, **422** on validation, and an **opaque 500** that logs the stack trace only (no `user_id`, no driver/connection-string leak). Tests: `tests/test_api/test_feedback.py` (integration; 201/persistence/validation/auth/opaque-500). **Onboarding integration (frontend):** new-user OAuth now routes through `/dashboard` (not `/onboarding`) so the dashboard's completion check drives the redirect and completed users aren't dead-ended; `lib/hooks/useOnboardingStatus.ts` fetches `GET /api/v1/onboarding/status` and **fails open** (`isComplete=true` on error — never traps a user); `app/dashboard/page.tsx` redirects incomplete users to `/onboarding` unless a **per-user** localStorage skip flag (`onboarding_skipped:<userId>`, set by `?skipOnboarding=true`) is present; `/onboarding` added to `middleware.ts` protected routes. **Quickstart (frontend):** static `app/quickstart/page.tsx` documents all 8 stages (purpose/actions/outcome/route per stage) with anchor nav; broken `/docs` links fixed → `/quickstart` (onboarding completion screen + `OnboardingStep` Help). **Feedback widget (frontend):** `components/FeedbackWidget.tsx` — floating bottom-right widget (star rating, category, message) POSTing to `/api/v1/feedback` with `page_context = location.pathname`, **inline success/error states (no toast dependency)** and a 15s AbortController timeout; mounted in `app/layout.tsx` inside the authenticated block. **E2E (AC4/AC5):** `e2e/workflows/beta-journey.spec.ts` (`@beta-acceptance`) — onboarding routing scenarios, the full journey mapped to the **10 `BETA_ROADMAP.md` criteria**, feedback widget success/error, and a **p95<500ms** API check via new `PerformanceMonitor.measureP95` (measured calls assert `response.ok()` so a fast error can't pass); journey/routing/feedback mock the backend, the perf test hits the live stack and skips if unreachable. Verified: all 7 beta-journey tests pass against the full live stack. Frontend tests: `__tests__/lib/hooks/useOnboardingStatus.test.tsx`, `__tests__/app/quickstart.test.tsx`, `__tests__/components/FeedbackWidget.test.tsx`. **Beta limitations / known gaps:** `useOnboardingStatus` uses the app-wide placeholder auth token (`lib/auth-helpers.ts`) — under `SKIP_AUTH=false` the backend can't decode it, so the status check fails open and the redirect won't fire in production until real JWT exchange lands (app-wide, separate issue); E2E suite is **not** in the CI gate (manual `workflow_dispatch`); skip state is last-writer-wins across devices (#91).
- **Issue #153 (frontend cleanup — CSV export + feature-selection comparison view):** removed the two shipped TODO markers by implementing the features they described (frontend-only; no backend changes — `MethodComparisonResponse` already carried all the data). **CSV export:** `PreviewControls` gains an optional `onExport` callback (mirrors `onRefresh`); the Export button is disabled until a parent supplies it (and while `loading`). `TransformationPreview` builds the CSV from `preview_result.transformed_data` (header = union of row keys, preserving first-seen order) and downloads `transformation-preview-{datasetId}.csv`, reusing the RFC 4180-style field-quoting + Blob/anchor pattern from `SelectedFeatureSet.tsx`; array/object cells are `JSON.stringify`-serialized (never `[object Object]`), and a field is quoted when it contains `,`/`"`/`\n`/`\r`. Line endings are **LF** to match the existing `SelectedFeatureSet` convention (not strict RFC 4180 CRLF — accepted by all common parsers incl. Excel). **Comparison view:** `FeatureSelection` now preserves the full `MethodComparisonResponse` (instead of collapsing to the first method) in new `comparisonResult` state, adds a third **Comparison** tab (disabled until a comparison runs, auto-selected on success; `result`/single-method flow untouched), and renders the new `components/MethodComparisonView.tsx` (side-by-side method cards with selected features + top scores, prominent consensus features, a numeric pairwise overlap matrix, the `recommendations` text, and a CSV export of the comparison). Tests: extended `__tests__/components/transformation/PreviewControls.test.tsx` + `TransformationPreview.test.tsx` (export wiring, disabled states, real CSV content incl. complex-type + CR quoting), new `__tests__/components/MethodComparisonView.test.tsx` + `FeatureSelection.test.tsx` (3-tab structure, comparison state, auto-switch, service mocked). Incidental: removed a pre-existing unused `TransformationService` import in `TransformationPreview.tsx`. CodeRabbit (CLI + PR) findings addressed: JSON-serialize complex CSV cells, quote `\r`, props JSDoc. **Note:** the comparison flow still uses a fixed method set (`correlation`, `mutual_info`, `random_forest`, `statistical`), unchanged.
- **Issue #157 (stabilize flaky performance smoke tests):** the root cause was **wall-clock timing assertions inside the blocking `@smoke` gate** (`smoke-tests.yml`, runs every PR) — on shared 2-core GitHub runners `Date.now()` deltas around page loads / API calls are contention-sensitive, so identical code flaked. Fix is to **split timing tests out of `@smoke` into a new `@perf` tag** (decided over retuning thresholds): in `e2e/workflows/performance.spec.ts` the dashboard-load test is un-`fixme`'d, retagged `@perf`, threshold widened to a CI-realistic **5000ms TTI ceiling**; the single-prediction test is retagged `@perf` with the intended SLO documented as **1000ms** (was a 2000ms stability margin; ~180ms local). The blocking gate loses **no functional coverage** — functional prediction smoke already lives in `predict.spec.ts (@smoke)`. New non-blocking **`.github/workflows/perf-tests.yml`** runs `@perf` on `chromium-full` with **`continue-on-error: true`** (records latencies into the `performance-results` artifact, never blocks the PR; triggers on `performance.spec.ts`/`PerformanceMonitor.ts` changes + `workflow_dispatch`); `npm run test:e2e:perf` = `--grep @perf --project=chromium-full`. The two `transform.spec.ts @smoke` tests stay in `@smoke` (they assert *functional* transformation, not timing) and keep their `test.slow()` (from #156). Removing the two core-pegging perf tests from `@smoke` relieves contention but did not fully stabilize the gate — CI showed the remaining flakes are heavy **train-then-assert** `@smoke` specs (`evaluate.spec.ts`, `model-config.spec.ts`, `data-preparation.spec.ts`, `train.spec.ts`) whose upload/training in `beforeEach` exceeded the 30s test budget (hook/fixture setup counts against it) under 2-core worker contention; each now calls **`test.slow()` in its `beforeEach`** (same #156 pattern) to triple the budget. (`complete-ai-workflow.spec.ts` already had a file-level `test.setTimeout(300000)`.) No `playwright.config.ts` worker-count change, no `PerformanceMonitor`/backend changes. **Note:** at #157 time `smoke-tests.yml` was a non-required check (the required gate was `ci.yml`'s `CI Success` aggregate) and had been red on every recent PR from this same systemic 2-core starvation. **Superseded by #210:** the smoke gate was later folded into `ci.yml` as the `e2e-smoke` job and is now part of the required `CI Success` aggregate (`smoke-tests.yml` removed).
- **Issue #170 (InteractiveVisualizationDashboard renders real data, not `Math.random()`):** the dashboard generated chart payloads with `Math.random()` (`sampleChartData` memo: scatter/line/histogram) + a static `sampleBoxPlotData`; only correlation was real. **Frontend:** histogram now uses `HistogramChart`'s fetch-by-column mode (`datasetId`/`column`/new `bins` prop); boxplot/scatter/line use real services (`getBoxPlot`/`getScatterPlot`/`getLineChart`) via per-chart local fetch state + `getAuthToken()` + stale-response guards (data cleared on refetch/error so export can't serialize a stale selection); **line charts accept a non-numeric (datetime/text) X axis** — only the Y series must be numeric; explicit empty/unsupported guards per chart (never fabricates data); real-data export (`handleExportChart` serializes the active chart's fetched data; histogram re-fetches on demand); real refetch replaces the simulated `setTimeout`. Removed `sampleChartData`/`sampleBoxPlotData`. **Pre-existing bug fixed:** the dashboard's `Tabs` was hardcoded to `value="configure"`, so the **Visualize** tab (where charts render) could never display — made it controlled (`activeTab`). **Backend contract fixes exposed by wiring real endpoints:** scatter/line/timeseries loaded from `dataset.file_path` (a raw S3 key the downloader rejects → **500 on normal uploads**) — switched to `dataset.s3_url` (always usable; consistent with the histogram/boxplot/correlation cache service); the line route returned a raw `numpy` x → FastAPI 500, now coerced to native int/float/str; `getHistogram` sent `?bins=` but the route param is `num_bins` (custom bin counts were silently ignored) — fixed; the histogram cache key omits `num_bins`, so only the default count is cached and non-default counts compute fresh (a cached default entry is validated against its bin count before serving — self-heals pollution); `getBoxPlot`/`getScatterPlot`/`getLineChart` now `encodeURIComponent` column path segments (handles spaces/`#`/`&`; a literal `/` in a column name still 404s — Starlette decodes the path before routing — pre-existing). **Demo-found fix:** `/user_data/{id}` returns the id as `_id` with `id:null`, so the explore page passed `datasetId={null}` to the dashboard and no chart could fetch — `app/explore/[id]/page.tsx` now normalizes `dataset.id` from `_id` (route-param fallback) on load and when merging processing results. Tests: `__tests__/components/InteractiveVisualizationDashboard.test.tsx` (11 cases), extended `HistogramChart.test.tsx`/`visualization.test.ts`, backend `test_api/test_visualizations.py` (s3_url + numpy-x) + `test_services/test_visualization_cache.py` (bin-count bypass + stale self-heal), explore `page.test.tsx` (id normalization). **Beta limitation:** visualizations load the **original uploaded** dataset (`s3_url`); showing the post-transformation version requires normalizing the `file_path` key/URL inconsistency across upload+transformation **and** a source-aware viz cache — deferred (verified that naively preferring `file_path` 500s every untransformed dataset).
- **Issue #168 (deploy page contract mismatch — frontend):** `app/deploy/page.tsx` spoke a contract the backend never had, so deploying from the UI silently never worked. **Fixes:** `handleDeploy` now uses **`PUT /models/{id}/deploy`** (was POST → wrong route) and reads `deployment_endpoint`/`model_id` (was `api_endpoint`/`api_key`/`id`, none of which `ModelDeployResponse` returns); `completeStage` records `model_id`+`deployment_endpoint`. Because `mark_deployed(endpoint)` **persists the endpoint verbatim and synthesizes no default**, the UI supplies the production serving URL (`${API_URL}/production/v1/models/{id}` — where `production_predict` is registered) as `endpoint`, else the success page would show a `null` endpoint. `checkDeploymentStatus` previously hit a **nonexistent** `GET /models/{id}/deployment` (silent 404 on mount) → now reads `deployment_config.{is_deployed,deployment_endpoint,deployed_at}` from `GET /models/{id}`. Success UI drops the **API Key** section (backend issues none on deploy; keys come from account settings) and the example `curl` now uses the real production contract (`X-API-Key` header, `{ "data": [...] }` body — `ProductionPredictRequest`, `verify_api_key`). `lib/types/api.ts` `DeployResponse` now **mirrors backend `ModelDeployResponse`** (`{model_id,status,deployed_at,deployment_endpoint:string|null,message}`) with a narrow `ModelDeploymentView` for the status check, so future drift fails `tsc`. Tests: `__tests__/app/deploy/page.test.tsx` (PUT+endpoint-in-body, status-check route, real endpoint render, no api-key section, stage completion). **Out of scope:** `lib/services/visualization.ts` `getBoxPlot`/`getCorrelationMatrix` share the same camelCase drift but have **no callers** (`useVisualizations` unused) — deferred (YAGNI).
- **Issue #84 (model deployment as production REST API — post-beta V2):** built on the **real `MLModel`/`/api/v1/ml/`** surface; the Traycer 14-step plan was badly over-scoped (a new `Deployment` Beanie doc, `DeploymentService`, dynamic FastAPI route registration, a new rate-limit middleware, per-deployment API keys, monitoring routes, a version-routing/canary/alias engine) and targeted the dead `ModelConfig`/`/api/v1/models/` surface. **Root cause it fixed:** the deploy button (#168) called `PUT /api/v1/models/{id}/deploy` (`ModelService` → `ModelConfig`), which is **never created by the real training flow** (`model_storage.save_model` makes only `MLModel`) — so deploy **404'd against every real trained model**, unfixed by #168 (which only corrected the request/response *shape*, not the surface). Most ACs were already satisfied: **production serving** (`POST /api/v1/production/v1/models/{id}/predict`, `verify_api_key`, real inference), **API-key auth + rate limiting** (#151), **OpenAPI docs** (FastAPI `/docs` + `api_documentation.py`), and **multiple versions** (#78: each version is a distinct `MLModel.model_id` = its own live endpoint). **Changes:** `MLModel` gains optional `is_deployed`/`deployment_endpoint`/`deployed_at` (pre-#84 models degrade to not-deployed; `sample_ml_model` mock fixture updated — recurring `response_model=MLModel` gotcha); new `PUT /api/v1/ml/{model_id}/deploy` in `model_training.py` (registered **before** the catch-all `/{model_id}`; reuses `ModelDeployRequest`/`ModelDeployResponse`) marks the model + **synthesizes the production serving URL from `request.base_url`** when `endpoint` is omitted (404 unknown/foreign); status is read via the existing `GET /api/v1/ml/{model_id}`. Frontend: deploy page repointed `/models/{id}` → `/ml/{id}` (deploy + status, reading top-level fields; `ModelDeploymentView` in `lib/types/api.ts` updated to top-level shape); new `components/EndpointTester.tsx` (AC4) sends a live request to `{endpoint}/predict` with an `X-API-Key` reusing the `getModelFeatures` auto-form; real-feature **type-aware** curl example (uses the first allowed categorical option) + a "View interactive API docs" link to the Swagger UI at the **host root `/docs`** (`API_URL.replace(/\/api\/v1\/?$/, '') + '/docs'` — the production route keeps `/api/v1`, but Swagger UI is mounted at root; a codex/CodeRabbit "double prefix" flag on `servingEndpoint` was a **false positive** — `/api/v1` mount + `/production` router + `/v1/models` route is the genuine path). Tests: `tests/test_api/test_ml_deploy.py` (real Mongo: deploy marks + synthesizes endpoint, status on GET, supplied-endpoint honored, 404 unknown/foreign), `__tests__/components/EndpointTester.test.tsx` + updated `__tests__/app/deploy/page.test.tsx`. **Beta limitations:** serving is **not gated** on `is_deployed` (any trained model is callable with a valid scoped API key — matches #82/#83 predict); no undeploy endpoint, no canary/traffic-splitting, no separately-provisioned infra (the deploy-page "auto-scaling/cost" copy is illustrative).
- **Issue #85 (deployment monitoring dashboard — post-beta V2):** the issue's "Current State" was stale — the monitor **overview page already showed real data**, prediction-distribution tracking already existed, and the in-memory `prediction_log` + `/monitoring` routes (keyed by `model_id`) already aggregated metrics. The **real gaps**: `production_predict` had a `# TODO` and **never logged** (so deployed-model metrics were empty), and `error_rate` was hardcoded to `0`. **Scope kept lean** (the Traycer plan over-scoped 3 new MongoDB time-series collections + cost tracking + anomaly detection + alert-rule CRUD + a parallel `/deployments` router): reuse the in-memory log + `/monitoring` router (a deployed model **is** an `MLModel` by `model_id`, per #84 — no separate Deployment entity). **Backend:** `production_predict` now logs every prediction (success → one entry per record) and failures (**one error entry per input record** so batch error-rate stays per-record consistent — codex review fix) via best-effort `_record_serving_metrics` (`production.py`, in-memory only, never raises/blocks the hot path; batch wall-clock latency split `latency_ms/n` — a documented beta approximation). `PredictionLog` entries gain an optional `error` field; `PredictionMonitoringService` adds a pure `_compute_metrics(events, hours)` (real `error_rate` + `error_count` + `latency_percentiles` p50/p90/p95/p99 over successes; errors excluded from distribution/confidence), `get_usage_timeline` (bucketed requests/errors/avg-latency, empty buckets included, auto granularity), and `get_health` (status healthy/degraded/unhealthy/unknown + threshold alerts `DEGRADED/UNHEALTHY_ERROR_RATE` 5%/20%, `..._LATENCY_MS` 1000/5000; reuses its loaded window via `_compute_metrics`, no double scan). New endpoints `GET /api/v1/monitoring/models/{id}/timeline` + `/health` (registered before `/distribution`); `/metrics` response extended with `error_count` + `latency_percentiles`. Incidental: fixed two latent 500s where the metrics/distribution responses omitted required fields for models with zero predictions. **Frontend:** `app/monitor/[id]/page.tsx` gains a health badge + active alerts banner, latency-percentile cards (Performance tab), a `LineChart` usage timeline (new Timeline tab), and a `BarChart` distribution (replacing manual `<div>` bars); `ProductionService.getUsageTimeline`/`getDeploymentHealth` + `LatencyPercentiles`/`TimelineBucket`/`UsageTimeline`/`HealthAlert`/`DeploymentHealth` types. **Schemas inline in `monitoring.py`** (consistent with that file's existing `ModelMetricsResponse` etc., not `app/schemas/`). Tests: `test_prediction_monitoring.py` (percentiles/error_rate/all-errors/timeline/health states), `test_production.py::TestServingMetricsLogging` (per-record success/error logging, batch error granularity, never-raises), `test_monitoring.py` (timeline/health endpoints + response-format), `__tests__/lib/services/production.test.ts`, `__tests__/app/monitor/detail.page.test.tsx`. **Beta limitations:** metrics are **in-memory / process-local** (reset on restart — issue header: "Beta can rely on basic logging"; upgrade path = swap `prediction_log` for a Beanie time-series collection behind the same service API); no cost tracking, no anomaly/drift expansion, no alert-rule CRUD/notification channels, no parallel `/deployments` router. A `datetime.utcnow()` "CI blocker" flagged by `claude-review` was a **false positive** (`UP017` targets `timezone.utc`, not `utcnow()`; CI green).
- **Issue #86 (client SDKs & integration tools — P5.6, post-beta V2):** generates **per-deployment** client SDKs on the **real `/api/v1/ml/`** surface against the real production serving contract (`POST {endpoint}/predict`, `X-API-Key`, `{"data":[{<feature>:...}]}`), **not** the over-scoped Traycer plan (dropped a `/deployments/{id}/` dead-surface router, `Webhook`/`WebhookDelivery` Beanie docs + `WebhookService` + 5 CRUD routes, async-predict endpoint + TTL job store, an SDK test-runner shelling out to `python`/`tsc`, Redis SDK caching, version tracking, download analytics, monitoring sdk-usage, zip packages, jinja2 templates). The pre-existing generic `APIDocumentationService` (`/api/v1/docs/clients/{lang}` etc.) hardcoded a fake host + wrong predict route + Bearer auth and is **left untouched**. New stateless `SDKGenerator` (`app/services/sdk_generator.py`, pure/never-raises): per-deployment Python / **TypeScript** (the genuine gap) / JavaScript / cURL clients, per-deployment Postman collection, flask/fastapi/express/nextjs + webhook-receiver framework samples, and per-SDK README (AC5) — all parameterized by `(model_id, model_name, feature_names, problem_type, serving_endpoint)` with the model's real `feature_names` in the sample record. Routes in `model_training.py` (registered **before** catch-all `/{model_id}`, owner-scoped, 404 foreign): `GET /ml/{id}/sdk` (info), `/sdk/postman` (before `/sdk/{language}` so "postman" isn't captured as a language), `/sdk/{language}` (`text/plain`, 404 unknown language). Serving base URL = persisted `deployment_endpoint` (#84) else synthesized from `request.base_url`. **AC4 webhooks — lean:** optional `webhook_url`/`webhook_secret` carried in the existing batch job's `config` (`BatchPredictionConfig`, **no `BatchJob` migration**); on terminal state `BatchPredictionService._fire_webhook` fires a best-effort HMAC-SHA256-signed (`X-Signature`) `httpx` POST of the job summary — **never blocks/raises**, retries once. **Codex-review hardening:** `_is_safe_webhook_url` SSRF guard (http(s) only, rejects hosts resolving to loopback/private/link-local/reserved — blocks `127.0.0.1`, `169.254.169.254` metadata, RFC1918) and `response.raise_for_status()` so non-2xx deliveries are actually retried (httpx doesn't raise by default). Batch route (`batch_prediction.py`) gains two optional webhook `Form` fields. Frontend: `ModelService.getSdkInfo/getSdk/getSdkPostman` + `SdkInfo` type; `components/SdkPanel.tsx` on the deploy page beside `EndpointTester` (language tabs, copy/download, Postman download). Tests: `test_sdk_generator.py`, `test_api/test_sdk_routes.py` (200/404-language/404-foreign), extended `test_batch_prediction.py` (signed/unsigned/never-raises/non-2xx-retry/SSRF-block), `SdkPanel.test.tsx` + extended `model.test.ts` (the deploy page test stubs `SdkPanel`). **Review hardening (claude-review):** user-controlled `model_name` is sanitized (`_safe_display_name`, strips `"""`/`*/`/backtick/`$`/newlines) before embedding in generated source so a hostile name can't break/inject the SDK; the SSRF DNS resolve runs in `asyncio.to_thread` (never blocks the loop); `webhook_secret` is masked (`****`) in the batch job-status `config` responses (`_redact_config`) so the signing key isn't echoed back. **Beta limitations:** SDKs are single-file source (copy/download), not pip/npm-published packages; **generated sample records default every feature to `0`** (placeholder — categorical columns need a real label, so a copy-pasted example may 422 until edited; the typed schema is at `GET /ml/{id}/features`); webhooks fire only on **batch** completion (no sync-predict callback, no webhook management UI/CRUD, no delivery-log persistence, SSRF guard is not DNS-rebinding-proof — egress allowlist is the real fix; no outbound per-user/per-host webhook rate limit — a user could fan out many jobs at one target, so add a cap before GA); `webhook_secret` is stored **plaintext at rest** in the job document (masked in API responses, but the server needs the raw key to HMAC-sign at fire time, so hashing isn't possible — encrypt-at-rest is the future hardening); generic `/docs/*` surface untouched.
- **Issue #162 (history types camelCase vs API snake_case — frontend):** the backend `GET /transformations/datasets/{id}/history` (`HistoryDataResponse`) emits **snake_case** entry fields (`transformation_type`/`affected_columns`/`rows_affected`/`version_id`) but the frontend `HistoryEntry` type + `HistoryItem.tsx` consume **camelCase**, and `HistoryService.getHistory` returned the raw JSON — so the first consumer to wire up the history UI would silently get `undefined` entry fields (no live consumer today, so nothing was broken yet). **Fix (service-layer mapping, not type churn):** new private `mapHistoryResponse` in `lib/services/history.ts` maps each entry to camelCase (top-level `current_position`/`can_undo`/`can_redo` already matched the frontend `HistoryData`, so they pass through), defaults `affected_columns` to `[]`, and normalizes `null` `rows_affected`/`version_id` to `undefined`; `getHistory` returns the mapped result. Tests: `__tests__/services/history.test.ts` now mocks the **real snake_case payload shape** and asserts camelCase output + no snake_case key leakage (the transformation-history component tests were unchanged — they already use the post-mapping camelCase shape).
- **Issue #89 (AI decision engine for tool selection & parameter optimization — P5.7, post-beta V2):** built **lean** on the real `DatasetMetadata`/metadata surface — **one** cohesive `AIOrchestrationService` (`app/services/ai_orchestration_service.py`, singleton, never raises) instead of the CodeRabbit plan's ~13 files (dropped the 7-file engine split, `practices.json`/`KnowledgeBase`/`BestPractice`, separate `DataProfileBuilder`/`FeedbackAnalyzer` modules, the `personalization` "learning loop"). **Rule-based core + optional OpenAI enhancement** mirroring `EvaluationExplanationService` (#79): the endpoints work **fully without an OpenAI key** (rule-based `generated_by="rule_based"`); when a key is present OpenAI enhances only the plain-language `data_profile_summary` behind the shared `@with_circuit_breaker("openai")` (`generated_by="hybrid"`). Endpoints under `/api/v1/ai` (new `ai_orchestration.router`, coexists with `ai_analysis` at the same prefix — no path collision): `POST /recommend-tools` (ranked `ToolRecommendation`s per `Objective` enum — data_cleaning/feature_engineering/modeling/exploration — + ordered multi-stage `pipeline_suggestion` via `_STAGE_ORDER` + `reasoning_trace` + `personalization_applied`/`partial`), `POST /optimize-parameters` (rule-based params + alternatives), `POST /feedback` (persists `AIRecommendationFeedback`). Auth `Depends(get_current_user_id)`; recommend/optimize **404 on unknown/foreign dataset**, feedback validates dataset ownership when `dataset_id` is supplied. `build_profile` aggregates `DatasetMetadata.data_schema` (+ **falls back to `inferred_schema`** — the `/api/v1/datasets` upload path stores `data_schema=[]` with real columns in `inferred_schema`, a `SchemaInference` dump) and the **newest** `DataIssueRecord` (sorted `-detected_at`; `store_detection_results` inserts a fresh record per run). New `AIRecommendationFeedback` Beanie doc (`app/models/ai_feedback.py`, registered in `registry.py`); schemas in `app/schemas/ai_orchestration.py`. **Executable-contract alignment (codex review):** recommendations/optimized params use the transformation engine's real contract — `FILL_MISSING` + `method` (not the unmapped `IMPUTE_MEDIAN`/`strategy`), `method=median` for numeric vs `method=mode` for categorical (FillMissing only mean/median on numeric), an **advisory** `drop_columns` (not `drop_missing`, which drops ROWS and is rejected past 50% loss) for >50%-empty columns, and `optimize-parameters` is **column-aware** (median-vs-mode and onehot-vs-label keyed off the caller's requested `columns`, not dataset-wide). All documented constraints honored (`interpretability_preference` high/low, `time_budget` fast/thorough) — each recorded in `reasoning_trace` so an ignored constraint can't pass silently. **"Learning"** = beta-level feedback **counts** (net accepted−rejected per tool type → ±1 priority nudge), not an ML loop. Tests: `tests/test_services/test_ai_orchestration_service.py` (rule engine per objective, pipeline ordering, column-aware param optimization, constraints, no-key path), `tests/test_api/test_ai_orchestration.py` (endpoints, ownership 404s, feedback persistence + personalization loop, newest-issue-record, inferred_schema fallback). **Beta limitations:** frontend deferred (issue is backend-labeled); per-column imputation uses a robust median default (no `ColumnStats` skew lookup); OpenAI enhancement is summary-only (recommendations themselves are deterministic); personalization is global feedback counts (no recency weighting / multi-device sync). codex converged clean after 8 review rounds.
- **Issue #90 (AI assistance across all 8 workflow stages — P5.8, post-beta V2):** built **lean** by extending the **existing** #89 `AIOrchestrationService` — the Traycer plan was stale/over-scoped (its centerpiece `ai_orchestration_service.py` already shipped in #89; it wanted 5 more `*_ai_service.py` services, 5 frontend components + `ContextualAIHelp`, a separate `ai_prompts.py`/`ai_responses.py`, Redis caching, and per-user rate limiting — all dropped). The real gaps vs the 4 ACs: 6 of 8 stages already had backend AI (profiling #2, data-prep/feature-eng via #89 `recommend-tools`, model-selection via `explanation_service`, evaluation via #79 report card + #81 errors), so the gaps were **Stage 8 deployment (no AI at all)**, **no shared personality/tone**, and **no cross-stage context accumulation**. **One new capability — stage guidance:** `POST /api/v1/ai/stage-guidance` (registered in `ai_orchestration.router`; auth, **404 unknown/foreign dataset**, never 500). `AIOrchestrationService.generate_stage_guidance(profile, stage, request_context, user_id)` returns a `StageGuidanceResponse` (`focus`, `guidance_summary`, `key_considerations[]`, `suggested_actions[]`, `context_used[]`, `reasoning_trace[]`, `generated_by`, `partial`). **Consistent personality (AC2):** module-level `AI_MENTOR_PERSONA` prefixes the stage-guidance OpenAI system prompt. **Context accumulation (AC3):** `_accumulate_context` reads persisted `WorkflowState` (#87) best-effort (`WorkflowState.find_one`, swallows errors so it works DB-free in unit tests) for prior `completed_stages` + `stage_data`, then merges the request's `accumulated_context` on top; a leading consideration folds `context_used` into the **rule-based (no-key) path** too so it's genuinely context-aware, not just echoed (codex P2 fix). **Per-stage rule core (AC1, works without a key):** reuses `_cleaning/_feature/_modeling_recs` for stages 3/4/5; `_STATIC_STAGE_GUIDANCE` for stages with their own rich AI surfaces (loading/profiling/evaluation/prediction — complement, don't duplicate #79/#81/#83/#80); and a dedicated `_deployment_guidance` (real-time-vs-batch heuristic by `n_rows`, monitoring, rollback, pre-deploy checklist) — the genuine gap. **Optional OpenAI (AC1/AC4):** `_stage_guidance_summary` behind `@with_circuit_breaker("openai", fallback_value=None)`, JSON mode, persona-prefixed, enhances only `guidance_summary` (`generated_by` hybrid/rule_based); never raises. New schemas `WorkflowStageId`/`StageGuidanceRequest`/`StageGuidanceResponse` in `app/schemas/ai_orchestration.py` (no new model, no registry change). Tests: extended `tests/test_services/test_ai_orchestration_service.py` (all-8-stages parametrized, deployment gap, real-time-vs-batch, caller-context folds into considerations, `_summarize_decision` caps) + `tests/test_api/test_ai_orchestration.py` (shape, 404 unknown/foreign, **accumulation from persisted `WorkflowState`**). **Beta limitations:** frontend deferred (issue is backend/ml-core labeled — matches #89); accumulation reads only `WorkflowState.stage_data` shapes the workflow already persists (no schema for richer decision capture); rate limiting is the existing global #151 (no per-AI-endpoint cap).
- **Issue #101 (Quick/Comprehensive training modes — P5.9, post-beta V2):** built **lean** — the Traycer plan was ~70% stale (it wanted to rebuild progress tracking/time-remaining UI/polling from #76 and tuning budgets from #77, plus a `dataset_profiler.py`, `training_constants.py`, new `MLModel` DB fields, a `/ml/train/{id}/progress` endpoint + Redis progress cache, and `RecommendationBanner`/`TrainingProgressDialog` components — all dropped). The real gaps: a mode→config map, the engine's **`time_limit` was accepted but never enforced** (and the route never passed it), early-stop-on-good-score, a 10+ algorithm catalog, and the mode selector + recommendation. New stateless **`app/services/model_training/training_mode.py`**: `TrainingMode` enum (`quick`/`comprehensive`), `resolve_mode_config(mode, overrides)` → engine kwargs (explicit overrides win; unknown/None mode → `{}` so pre-#101 behaviour is unchanged), `recommend_mode(n_rows, n_features)` (size heuristic + reasoning). Quick preset = `max_models=3, time_limit=300, enable_tuning=False, early_stop_score=0.95`; Comprehensive = `max_models=12, time_limit=1800, enable_tuning=True, tuning_strategy="bayesian", tuning_config={"time_budget":120,"n_trials":20}, early_stop_score=None`. **`AutoMLEngine`** gains `early_stop_score`; the candidate loop now **enforces `time_limit`** (breaks between candidates once ≥1 model trained, never mid-fit) and **early-stops** when a candidate's `cv_score >= early_stop_score`, recording `early_stopped`/`stop_reason` (`time_budget_reached`/`target_score_reached`)/`algorithms_evaluated` on `AutoMLResult`. `_get_candidate_models` appends **always-on** sklearn algos (classification: ExtraTrees, AdaBoost, DecisionTree, GaussianNB; regression: ExtraTrees, AdaBoost, DecisionTree, Lasso, ElasticNet) **after** the top candidates so the comprehensive catalog hits **10+** while Quick's first-3 (LogReg/RF/XGBoost) is unaffected. Train route (`model_training.py`) resolves the mode from the existing **`training_config` dict** (no request-schema change, **no new `MLModel` field** → dodges the `response_model=MLModel` mock-fixture gotcha; mode rides in the persisted `training_config`), passes `time_limit` + `early_stop_score` + the mode's tuning defaults, and stamps `early_stopped`/`stop_reason`/`algorithms_evaluated` into `training_config`. New **`GET /api/v1/ml/datasets/{dataset_id}/mode-recommendation`** (registered before the catch-all `/{model_id}`; owner-scoped, 404 unknown/foreign; reads `UserData.row_count`/`num_rows` + `num_columns`). **codex-review fixes:** comprehensive's per-candidate tuning `time_budget` (120s) bounds the up-front tuning phase (which runs *before* the loop-level `time_limit` check) so 12×120s ≈ 24min stays within the 30min cap — without it `TuningConfig()`'s 600s/candidate default could run ~2h; frontend `modeTouchedRef` stops a late-arriving recommendation from overwriting a user's mode pick. Frontend: `TrainingModeSelector.tsx` (two trade-off cards + recommendation banner), wired into `app/model/page.tsx` (fetches `getModeRecommendation` on load, preselects it, sends `training_config.training_mode`); `model.ts` adds `TrainingMode`/`ModeRecommendation` types + `getModeRecommendation`. Tests: `tests/test_model_training/test_training_mode.py`, extended `test_automl_engine.py` (time-budget break, early-stop, 10+ catalog, Quick-top-3, backward-compat), `test_api/test_model_training.py::TestModeRecommendationEndpoint`, `__tests__/components/TrainingModeSelector.test.tsx`, extended `model.test.ts` + model `page.test.tsx`. **Beta limitations:** the 30min cap is approximate (a single grid/random sklearn search still can't be interrupted mid-call — #77 carryover; comprehensive uses bayesian which honours the budget); "10+ algorithms" holds for small/medium data (KNN/SVM/GradientBoosting are size-gated, and the time budget caps actual evaluated count); recommendation is a coarse rows/features heuristic; progress time-remaining is the pre-existing #76 UI.
- **Issue #102 (data quality scoring system — P5.10, post-beta V2):** built **lean** on the existing `QualityAssessmentService`/`TransformationLineage`/versions surfaces — the CodeRabbit plan was over-scoped (a new `QualityGate` Beanie model + CRUD + presets, a `QualityReportGenerator` with PDF export, a separate `ActionableRecommendation` model + a new `ISSUE_TO_TRANSFORMATION_MAP` duplicating the existing `FixSuggestionEngine`, configurable per-request dimension weights — all dropped). **AC1 (0-100 score):** `QualityReport` (`app/services/data_processing/quality_assessment.py`) gains `score_0_100` + per-dimension `component_scores` (0-100), equal-weight over `SCORE_DIMENSIONS` = the 5 AC dims (completeness/validity/consistency/uniqueness/accuracy; **timeliness excluded** as a hardcoded placeholder, accuracy is still the validity placeholder). No configurable weights (YAGNI). **AC2 (recommendations tied to transforms):** new `actionable_recommendations: list[ActionableRecommendation]` field — `_recommended_transformation` maps each `QualityIssue` to a canonical `TransformationType` (completeness→`fill_missing`, consistency-casing→`fix_casing`, consistency-numeric→`to_numeric`, consistency-date→`to_datetime`, validity-outlier→`outlier_removal`, uniqueness→`remove_duplicates`), grouped by (dimension, transform) with affected columns + max severity; existing text `recommendations` unchanged. **AC3 (trend across versions):** the previously-**dead** `TransformationLineage.quality_before/after/improvement` fields are now populated — `VersioningService.create_transformation_version`/`_create_lineage` take optional `quality_before`/`quality_after` dicts and compute `quality_improvement` = `after.score_0_100 - before.score_0_100`; `transformation_service.apply_transformation` computes both via `_assess_quality_dict` (best-effort, dtype→type map, `model_dump(mode="json")`, **never breaks the transform**). New `GET /api/v1/datasets/{dataset_id}/quality-trend` (`versions.py`, owner-scoped 404, never 500) via `VersioningService.get_quality_trend` (lineage oldest→newest, per-version before/after/improvement + transformation label + overall/best/worst). Schemas `QualityTrendPoint`/`QualityTrendResponse` in `app/schemas/version.py`. **AC4 (optional quality gates):** new **stateless** `app/services/quality_gate_service.py` (`evaluate_gates(report_dict)`, `overall_score`) with default `DEFAULT_GATES` (ML Training Ready: overall ≥70, completeness ≥80, validity ≥70) — **soft/advisory, `is_blocking` always False**, no DB model/CRUD; tolerates pre-#102 caches by deriving from 0-1 `dimension_scores`. **AC5 (report + dashboard):** `GET /api/v1/data/{file_id}/quality-report` (`data_processing.py`) aggregates score + components + actionable recs + gates from the cached `quality_report` (pre-#102 caches → `partial=true`, JSON only, no PDF); schemas in `app/schemas/quality.py`. Frontend: `lib/types/quality.ts` + `lib/services/quality.ts` (`getQualityReport`/`getQualityTrend`) + `components/quality/QualityDashboard.tsx` (0-100 gauge, gate status, component bars, actionable recs, version trend via the existing `LineChart`), rendered on the explore **Quality** tab (`fileId`=UserData id for the report, `datasetId` for the **best-effort** trend — the two IDs differ, so the chart hides when no trend exists). **Bug fixed in passing (blocked AC3):** `_create_lineage` built `TransformationStep(**step)` from `transformation_type`-keyed dicts (wrong field name) and the `step_type` validator only allowed 8 of 39 `TransformationType` values → a `ValidationError` (a `ValueError`) was swallowed into an `OperationError`, so **every transform on a dataset with a base version failed**; fixed by using the correct `TransformationStep` field names (`step_type`/`affected_columns`/`rows_affected`) in `transformation_service` + the validator now accepts all `TransformationType` values. CodeRabbit fixes: `TransformationStep.execution_time` persisted in **seconds** (was ms, ~1000× off); `QualityDashboard` clears stale `trend` on reload and drops null-`score_after` trend points (no fabricated 0). Tests: extended `test_quality_assessment.py`, new `test_quality_gate_service.py`, extended `test_versioning_service.py`/`test_transformation_service.py`/`test_version.py`, new trend+report API tests (`test_versions.py`/`test_data_processing.py`), `__tests__/components/QualityDashboard.test.tsx`. **Beta limitations:** equal-weight score; accuracy/timeliness still placeholders; quality assessed in engineered/loaded space via a coarse dtype map; report JSON-only; report keyed on UserData file id vs trend on DatasetMetadata id (dashboard fetches trend best-effort).
- **Issue #201 (honest calibration/eval split + out-of-sample calibration score — P5.11, from #83):** fixes the #83 beta limitation where `_calibrate_best_model` calibrated on the **same** held-out split that `_capture_evaluation_arrays` then measured the #79 dashboard metrics on, making both the dashboard numbers and `calibration_score` in-sample/optimistic. Scope = the issue's **preferred Option 1 (real fix)**, not the CodeRabbit caveat-flag-only plan; **batch-vectorization (the CodeRabbit Phase 3) is out of scope — it's #202**. `AutoMLEngine.run()` now carves a **disjoint calibration slice from the TRAINING set** for classification (`CALIBRATION_HOLDOUT_FRACTION=0.2`): base models fit/CV on `X_fit`, the best model calibrates on the carved `X_cal`, and both the calibration score **and** the #79 arrays are taken on the clean `X_test` (seen by neither base model nor calibrator) → honest metrics + an **out-of-sample** calibration score. `_can_carve_calibration` (instance method, uses `self.cv_folds`) guards: ≥2 classes, ≥2/class, slice ≥2·n_classes rows, and the conservative post-carve fit minority `int(min·0.8) ≥ cv_folds` so CV never goes NaN (codex), else **fallback** to the pre-#201 single-split (calibrate on the test set, in-sample). `_calibrate_best_model` returns `(is_calibrated, method, score, is_insample)` and, when the carved slice is degenerate (e.g. a stratified 18/2 carve lands all-majority → `calibrate_classifier` returns None), **falls back to test-set calibration rather than dropping calibration the deployed model had** (codex). `ConfidenceService.calibrate_classifier` gains optional `X_score`/`y_score` (scores out-of-sample on them when given, in-sample on `X_cal` otherwise; backward-compatible 3-tuple). New optional `MLModel` fields `calibration_score_is_insample` (default True so pre-#201 in-sample scores stay labelled) + `evaluation_on_calibration_set` (default False; True only on the small-data fallback) flow through `model_storage.save_model` and the train route's `model_metadata`. `ModelEvaluationResponse` (+ `lib/types/evaluation.ts`) gains `evaluation_on_calibration_set`, set on full **and** partial responses; `EvaluationExplanationService.generate_report_card` takes an optional `evaluation_on_calibration_set` and **prepends a single concern** (covers both OpenAI and rule-based paths in one place) only when True. Tests: `test_confidence_service.py` (out-of-sample score), `test_automl_engine_evaluation.py::TestHonestCalibrationSplit` (carve guard incl. cv_folds + degenerate-slice fallback + honest/fallback flags), `test_model_evaluation.py` (flag + caveat surfaced), `test_evaluation_explanation_service.py` (concern injected/absent), `sample_ml_model` mock fixture +2 fields (recurring `response_model=MLModel` gotcha). **Beta limitations:** the honest split trains base models on ~20% less data (the inherent cost of a real calibration holdout); per the guard, very small or highly imbalanced datasets keep the pre-#201 in-sample behaviour (transparently flagged); regression is unchanged (no calibration; the test split was already clean).
- **Issue #202 (vectorize batch `_predict_chunk` inference — P5.12, from #83):** the batch-vectorization #201 deferred. `BatchPredictionService._predict_chunk` ran `transform`/`predict`/`predict_proba` **once per row** via `iterrows()` (~3·N sklearn calls per chunk); it's now a thin dispatcher: tries `_predict_chunk_vectorized` (one `transform`, one `predict`, one `predict_proba` over the whole chunk; assembly loop indexes into the arrays, no sklearn calls — ~3 calls/chunk, demoed at ~1000× fewer calls on a 1000-row chunk) and **falls back to `_predict_chunk_sequential`** (the original row-by-row loop, extracted verbatim) on any chunk-level exception, preserving per-row error isolation (one bad row or a batch-incompatible estimator doesn't sink the chunk). Per-prediction explanations **stay batched** via `_attach_explanations` (#80) — the CodeRabbit plan's "revert explanations to per-row" step was stale and would have regressed #80, so it was dropped. No output/schema/API change. `X_array = np.asarray(X_transformed)` is built **only when `include_explanations`** so the common path never densifies a (possibly sparse) feature matrix; fallback logs at `warning(..., exc_info=True)`. Tests: `test_predict_chunk_vectorizes_inference` (predict/predict_proba called once for a 3-row chunk), `test_predict_chunk_falls_back_to_sequential_on_batch_failure` (per-row predict count on fallback), `test_vectorized_and_sequential_paths_produce_identical_results` (output parity, default no-metadata config); the pre-existing `_predict_chunk` tests (confidence/low-conf/interval/explanation) already exercise the vectorized path since the dispatcher tries it first. **Beta limitations / deferred:** fallback is whole-chunk (no partial recovery); no per-job circuit-break to skip the vectorized attempt after a systematic failure (log-volume only, deferred to avoid shared mutable state on the singleton service).
- **Issue #256 (P0.6 security — staging ran wildcard credentialed CORS):** `docker-compose.staging.yml`/`.env.staging.example` set `ALLOWED_ORIGINS` — a name **nothing reads** — so the backend fell back to its `BACKEND_CORS_ORIGINS` default `["*"]`; with `allow_credentials=True` (`main.py`) Starlette **reflects the caller's Origin** + `Access-Control-Allow-Credentials: true`, letting any site make authenticated cross-origin requests. Fix follows the repo's pure-function config pattern (mirrors `validate_skip_auth`): new `resolve_cors_origins(raw, environment)` + `_parse_cors_origins` in `app/config.py` parse **JSON array *or* comma-separated** origins and **raise `ValueError`** on `"*"` (explicit or the unset/default) when `is_production_like()` (any `"*"` refuses even mixed with explicit origins) → the backend **fails to start** rather than silently running wildcard CORS; dev/test keep the `["*"]` default (DX). `Settings.BACKEND_CORS_ORIGINS` is a one-line delegation (Starlette reads `allow_origins` once at setup, so the guard fires at startup). Compose renamed backend var → `BACKEND_CORS_ORIGINS` with a `${…:?}` **fail-if-unset** guard; frontend service gets `ALLOWED_ORIGINS` (empty fallback is deliberate — an empty allowlist echoes no cross-origin ACAO = safe; the asymmetry vs the backend `:?` guard is intentional). `apps/frontend/middleware.ts` replaces the `|| '*'` arbitrary-Origin reflection with an `ALLOWED_ORIGINS` allowlist (echo only when listed) **plus `Vary: Origin`** on every response (both bots flagged Major — behind nginx, per-Origin ACAO without `Vary` lets a shared cache replay one origin's result for another; matches Starlette's built-in behavior). Tests: `tests/test_security/test_cors_config.py` (parse formats, all prod-like env names, unset/empty/wildcard/mixed refusal, dev/test exempt), extended `__tests__/middleware.test.ts` (allowlisted echo, off-list block, empty allowlist, `Vary` presence, OPTIONS preflight). Docs: `PRODUCTION_DEPLOYMENT.md`/`STAGING_DEPLOYMENT_GUIDE.md` updated to both var names. **Beta limitations:** "outside development" == `is_production_like()` (`production`/`prod`/`staging`/`live`/`release`); an **unset** env keeps wildcard for local DX (deploys always set `ENVIRONMENT`, and staging additionally can't start without `BACKEND_CORS_ORIGINS`); malformed values like `[https://a.com]` (unquoted brackets — neither valid JSON nor the documented comma format) parse literally.
- **Issue #257 (P0.7 deploy — staging S3 bucket/region env vars didn't match what the backend reads):** one logical S3 bucket was read under **three** names (uploads `AWS_BUCKET_NAME`/`S3_BUCKET_NAME` in `s3_service.py`, download allowlist `AWS_S3_BUCKET` — **raises `ValueError` if unset**, versioning `S3_BUCKET` — **silently defaults** to `narrative-modeling-uploads` in `config.py`) and the region under **two** (`AWS_REGION` vs boto3's native `AWS_DEFAULT_REGION`). Staging compose set only `S3_BUCKET_NAME` + `AWS_DEFAULT_REGION`, so downloads raised and versioning targeted the wrong bucket (write/read diverge). **Deploy fix (hard AC):** `docker-compose.staging.yml`/`.env.staging.example` fan a single `${S3_BUCKET_NAME}` + `${AWS_DEFAULT_REGION}` source out to **every** name the backend reads (`AWS_BUCKET_NAME`/`AWS_S3_BUCKET`/`S3_BUCKET`/`AWS_REGION`; `S3_BUCKET_NAME` gains a `${…:?}` fail-if-unset guard) — one source of truth, no drift. **Config consolidation (ideal AC), mirrors #256's pure-function pattern:** new `resolve_s3_bucket()` (reads the bucket from any of `AWS_BUCKET_NAME`→`AWS_S3_BUCKET`→`S3_BUCKET`→`S3_BUCKET_NAME`, strips/skips blanks, canonical = `AWS_BUCKET_NAME`) and `resolve_aws_region()` (prefers `AWS_REGION`, falls back to boto3's `AWS_DEFAULT_REGION`) in `app/config.py`. The download allowlist (`s3_service.download_file_from_s3`) uses **own-var-first (`AWS_S3_BUCKET`) → `resolve_s3_bucket()` canonical fallback → fail-closed** (raises when nothing resolves; explicit var still wins = backward compatible + keeps existing allowlist tests green). `Settings.S3_BUCKET` = `os.getenv("S3_BUCKET") or resolve_s3_bucket() or "narrative-modeling-uploads"` — resolution only, **not** import-time fail-closed: codex suggested refusing the legacy default in prod-like envs (mirror the #256 CORS guard), but that raised at import and broke subprocess import tests of *other* prod-like guards (`test_skip_auth_guard.py`) since `config` is imported everywhere; a truly bucket-less deploy also breaks uploads and surfaces `NoSuchBucket`, so best-effort resolution is the right trade-off. `create_s3_client` region now via `resolve_aws_region()`. Tests: `tests/test_security/test_s3_config.py` (resolvers, precedence, blanks, prod fail-closed), added canonical-fallback case to `tests/test_services/test_s3_security.py`. **Beta limitations / deferred:** full resolver-in-every-upload-site is deferred (issue's "ideal" tier) — upload paths (`utils/s3.py`, `user_data.py`, `upload.py`, `column_stats.py`, `health.py`) still read `AWS_BUCKET_NAME` directly (= the resolver's canonical name, so a deploy setting it — as compose does — works; a deploy setting *only* `S3_BUCKET` would break uploads, which compose prevents by fanning out every name).
- **Issue #258 (P0.8 security — staging published backend/frontend/redis on `0.0.0.0`, bypassing nginx/TLS):** `docker-compose.staging.yml` published `8010:8000`/`3011:3000`/`6381:6379` on all interfaces, so on the shared VPS the API/app/redis were reachable over **plaintext HTTP at the host IP**, past nginx TLS/HSTS/security headers (nginx upstreams only to `127.0.0.1`). Firewall alone can't fix it: Docker's iptables `DNAT` is evaluated **before UFW**, so a public bind leaks even without a `ufw allow` rule. **Fix:** bind all three published ports to `127.0.0.1` (`127.0.0.1:8010:8000` etc.); loopback binding — **not `ufw allow`** — is the control that holds. Non-breaking: `nginx-staging.conf` already upstreams to `127.0.0.1:8010`/`3011` and `deploy.yml`'s post-deploy health check hits `localhost:8010` on the host. New `tests/test_security/test_staging_ports.py` parses the real compose file and fails if any published port re-exposes `0.0.0.0` (mirrors #256/#257's `test_security/` deploy-config guards); `docs/deployment/STAGING_DEPLOYMENT_GUIDE.md` Step 8 documents why loopback binding is the real control. **Beta limitations:** config/docs-only, no runtime code; the regression guard is a static YAML parse (not a live socket test, which would need Docker in CI).
- **Issue #259 (P0.9 privacy — dataset deletion was non-cascading; no right-to-erasure):** `DatasetService.delete_dataset` removed only the `DatasetMetadata` row, orphaning the S3 source object, the **dual-written legacy `UserData`** (PII: `contains_pii`/`pii_report`/`data_preview`), every child doc keyed by `dataset_id`, and per-model S3 artifacts. The app has **two parallel dataset id-spaces with no id link**: `DatasetMetadata` (string `dataset_id`, S3 `datasets/{user}/{id}_{file}`) with string-keyed children (`transformation_configs`/`dataset_versions`/`transformation_lineages`/`feature_definitions`/`workflow_states`/`bulk_transformation_jobs`/`ml_models`/`model_configs`/`training_jobs`/`data_issues`/`ai_recommendation_feedback`), and legacy `UserData` (ObjectId) with `Link[UserData]` DBRef children (`column_stats`/`visualization_cache`/`plot`/`analytics_result`/`trained_model`/`revised_data`); **`create_dataset` dual-writes both**, linked ONLY by shared `(user_id, s3_url)`. New stateless `DatasetErasureService` (`app/services/erasure_service.py`, singleton): ordered, idempotent, **best-effort** cascade (real Mongo txns need a replica set the local/CI tier lacks — `model_versioning_service.py:117` already documents avoiding them) across both id-spaces + per-model S3 via the existing `model_storage.delete_model` + Redis viz-cache (`redis_cache.delete_pattern("viz:{id}:*")`); returns a `DeletionManifest` (per-collection counts, S3 keys, redis evictions, `failures`, `notes`, `erasure_id`). **Root-cause fix:** `delete_dataset` delegates to `erase_dataset`, so the existing `DELETE /datasets/{id}` cascades for all callers. New `POST /datasets/{id}/erase` + `POST /users/me/erase` (`reason` in an `ErasureRequest` body, `max_length=500`) return the manifest and append an **immutable `ErasureAuditLog`** (`app/models/erasure_audit.py`, insert-only, registered in `registry.py`). **Security (self-review):** the cascade **gates every child sweep on an ownership-checked parent** (a non-owner erase is a no-op — child collections aren't all user-scoped, so without the gate a caller could erase another tenant's children); a pure no-op writes **no** audit entry (can't spam the append-only log). **Design:** parent deleted LAST and retained as a re-discoverable **tombstone** only when a **Mongo child** delete failed (S3/Redis failures are recorded but don't block Mongo deletion — users expect `DELETE` to remove the dataset; orphaned S3 PII is manifest-recorded + covered by AC3 lifecycle + re-runnable erase); tombstone scoping is per-dataset (`failures_start`) so one dataset's failure can't retain another's parent in `erase_user`. **AC3 (Atlas backups + S3 versioning/lifecycle + tested restore drill)** delivered as `docs/deployment/DATA_ERASURE_AND_BACKUP_RUNBOOK.md` + a follow-up ops issue — cloud config with no IaC in-repo, not CI-verifiable. Tests: `tests/test_integration/test_erasure_cascade.py` (zero-residual across both id-spaces + **dual-write twin** + cross-tenant no-op + idempotency; S3 forced mock for determinism, real LocalStack S3 delete exercised in the CI integration job), `tests/test_services/test_erasure_service.py` (id-space/S3-key helpers), `tests/test_api/test_erasure_api.py` (manifest shape + `erasure_id`↔audit correlation + foreign no-op writes no audit); `scripts/demo_erasure_259.py` is an ops verification tool. **Cross-family review:** opencode/GLM timed out on the ~1.1k-line diff; `codex review` was the review of record and caught the P1 dual-write orphan (the issue's core bug) + the aggregate-tombstone scoping bug. **Beta limitations:** best-effort (not ACID) — idempotent re-run clears residuals, manifest records failures; `batch_jobs`/`ab_tests` are model-scoped (swept by `user_id` in erase-user, left by erase-dataset); `erase_user` is sequential + reuses `redis KEYS`-based `delete_pattern` (fine at beta scale); `POST /users/me/erase` erases the user's data, not the auth/account record.
- **Issue #262 (P0.12 — A/B Testing advertised in main nav but unreachable end-to-end):** the Sidebar linked `/experiments` (full UI: list/new/[id]), but `lib/services/abTesting.ts` had two independent bugs that 404'd every request: a **relative** `baseUrl='/api/v1/ab-testing'` (resolved against the frontend origin — no rewrite/route) and `localStorage.getItem('authToken')` (a key nothing writes → always `Bearer null`). The backend route (`/api/v1/ab-testing`, `get_current_user_id` NextAuth-JWT auth) was already fully implemented — **frontend-only fix**. Refactored to the shared `API_BASE_URL` (`NEXT_PUBLIC_API_URL || 'http://localhost:8000/api/v1'`) + `getAuthToken()` JWT pattern used by `model.ts`/`transformation.ts`, via a private `authHeaders(json?)` helper applied to all 9 methods; dropped two now-vestigial `new URL(..., window.location.origin)` base args (first arg is now absolute). `app/experiments/page.tsx` no longer swallows the load error — it renders a Retry banner (`role="alert"`) so a failure isn't a silent empty page (the exact symptom the issue named). Chose the "make it beta-ready" AC path over the fallback of gating the Sidebar behind a flag. Tests: `tests/test_api/test_ab_testing.py` (real-Mongo integration — create→list round-trip through the real route with auth, unknown-model 400, **401/403 without auth** proving the JWT is load-bearing) + `__tests__/lib/services/abTesting.test.ts` (service targets the absolute backend URL with a `Bearer <jwt>`, regression-guards against relative URL + `Bearer null`, surfaces non-ok). **Beta limitations:** no Playwright E2E click-through (creating an experiment needs a trained `MLModel` + a real OAuth session the smoke stack doesn't provision — the real-backend integration test covers the same create→list→auth path deterministically); error surfacing added to the list page only (the `[id]`/`new` pages keep their existing per-action handling).
- **Issue #263 (P0.13 — data export endpoint was a stub returning a 404-bound download URL):** `POST /api/v1/data/{file_id}/export` (`data_processing.py`) had a `# TODO`, returned `status:"export_ready"` + `download_url=/api/v1/data/{file_id}/download?format=...` — a route that **doesn't exist** (404s), for csv/excel/json/parquet alike. **Chose the "implement real export" AC branch** (not endpoint removal): the endpoint now downloads the source from S3 (`parse_s3_url` → `s3_service.download_file_bytes`, same shapes the preview endpoint handles), reads it into a DataFrame via `_read_source_dataframe` (csv/**txt**-as-TSV/excel/**parquet**/**json** sources — matching every real upload path), serializes to the requested format via `_serialize_dataframe` (csv/json/parquet/excel; pyarrow + openpyxl already deps), uploads the artifact to `exports/{user_id}/{file_id}/{name}_processed.{fmt}`, and returns a **working presigned S3 GET URL** (1h) — no `/download` route needed, the presigned URL is directly browser-downloadable (`window.open` in `explore/[id]/page.tsx`). New reusable `s3_service.generate_presigned_url(key, expires_in=3600, filename=None)` (the service already holds the client + bucket; mirrors the presign pattern in `upload.py`; optional `filename` sets `ResponseContentDisposition: attachment; filename=...` so the browser downloads with the clean `{name}_processed.{fmt}` name instead of the raw S3 key). **Security (review):** `original_filename` is user-controlled, so the export key stem is sanitized with `os.path.basename(...)` — a crafted name like `../../admin/secret.csv` can't escape the `exports/{user_id}/{file_id}/` prefix. **Honesty fixes:** any S3/read failure now → **500 (never a false `export_ready`)**; an unreadable source type → 422; a source over `MAX_EXPORT_SOURCE_BYTES` (100 MB) → **413** (best-effort `head_object` size check before the download, since export reads+reserializes the whole file ~3x in memory). The blocking pandas read/serialize runs via `asyncio.to_thread` (same off-loop pattern as MCP #255) so large files don't stall the event loop. **Dropped the unused `include_stats` query param** (zero callers — the frontend only ever sends `{format:'csv'}` as a *body*, which FastAPI ignores since `format` is a query param — and it had no clean cross-format semantics; half-implementing it would repeat the same "report success for unimplemented" sin). Tests: `test_data_processing.py::test_export_data_produces_working_download_url` (parametrized over all 4 formats — asserts the artifact is uploaded, the returned URL **is** the presigned one, and the dead `/download?format=` link is gone) + `test_export_data_fails_loudly_on_s3_error` (500, no `export_ready`). **Beta limitations:** exports the **source** file re-serialized (same as the preview endpoint — there's no separate post-transformation artifact in `UserData`'s S3 space); presigned URL expires in 1h; artifacts accumulate under `exports/` (no lifecycle cleanup — covered by the #259 S3-lifecycle runbook).
- **Issue #264 (P1.1 security — production predict twin mapped client errors to 500, leaked raw exceptions, no record cap):** the public `POST /api/v1/production/v1/models/{id}/predict` (`production.py`, `verify_api_key`, the least-trusted paying-customer path) wrapped all logic in `except Exception → HTTPException(500, f"...{str(e)}")` — so a client mistake (missing feature / unknown category) surfaced as a **500 leaking the raw traceback**, while its internal twin `POST /api/v1/ml/{id}/predict` (session-authed owner) already validated → 422 (#82/#83). Request `data` had **no length bound** (internal caps at `MAX_PREDICT_RECORDS=1000`) and inference ran **synchronously on the event loop**. **Fix mirrors the internal twin and goes stricter for the public trust boundary:** validate required raw features up front → **422** with the sorted missing list; map `(ValueError, KeyError)` from `feature_engineer.transform`/`predict` → a **fixed sanitized 422** (`"Invalid feature value(s) in one or more records."` — raw sklearn/pandas text `logger.info`'d server-side, **never returned**, since it can echo internal column names/indices + input values); reserve **500** for true faults with generic `detail="Prediction failed"` (no `str(e)`), guarded by an **`except HTTPException: raise`** before the catch-all so the 4xx client errors aren't re-wrapped into 500s (this also means client 422s skip `_record_serving_metrics(error=...)`, so user typos no longer inflate the #85 `error_rate` dashboard); `load_model` `ValueError` → **503** `"Model temporarily unavailable"` (the record was already found + ownership-verified, so a load failure is a broken artifact, not a missing model — 404 would misdirect operators; still no S3-path leak); `max_length=MAX_PREDICT_RECORDS` on `ProductionPredictRequest.data`; `predict`/`predict_proba` offloaded via **`asyncio.to_thread`**. The internal route's `_required_input_features` was **promoted to public `required_input_features`** (`model_training.py`) and the cap constant reused — one source of truth, no second copy (kept in `model_training.py`; no new shared module — YAGNI). Tests: `test_production.py::TestProductionPredictErrorHandling` (missing-feature 422, empty-data 422, predict-level + **transform-level** ValueError → sanitized 422, KeyError → 422, true-fault generic 500, load-failure 503 — each with **no-leak assertions**, the record cap at/over the max, happy-path asserting `confidence` runs end-to-end). **Cross-family `codex` pre-PR review** caught two residual leaks (echoing `{exc}` on the 422 and `str(e)` on the 404) — both fixed before opening. **Beta limitations:** the sanitized 422 no longer tells the caller *which* value was bad (logged server-side only; `GET /ml/{id}/features` exposes the typed schema for debugging); `feature_engineer.transform` is **not** yet offloaded to `to_thread` (out of #264 scope — the AC names only predict/predict_proba, and the internal twin doesn't either; follow-up if profiled as a real blocker); client-error logging includes raw feature values (fine for beta; truncate if this API ever serves PII/regulated data).
- **Issue #265 (P1.2 perf — model re-downloaded + joblib-deserialized on every prediction; S3 'async' methods blocked the event loop):** `ModelStorageService.load_model` (re-instantiated per request, so no instance cache survived) unconditionally downloaded + `joblib.load`-ed the estimator **and** feature transformer from S3 on **every** prediction, and `S3Service`'s `async` methods called blocking boto3 directly on the asyncio loop — so per-request S3+pickle cost dominated latency and one slow download stalled all concurrent coroutines. **Cache (AC1):** new module-level `_ModelArtifactCache` in `model_storage.py` — a bounded **TTL-LRU** (`OrderedDict` + `threading.Lock`) of `(estimator, feature_engineer)` keyed by `(model_id, user_id)`; a hit serves with **no S3/Mongo round trip** (the key encodes `user_id`, so a hit is already ownership-scoped). Env-tunable `MODEL_CACHE_MAX_SIZE` (default 8) / `MODEL_CACHE_TTL_SECONDS` (default 900); either ≤0 disables. Invalidated via `invalidate_model_cache` on **delete** (`delete_model`), **retrain** (`save_model`, defensive — retrains mint a fresh `model_id`), and **deploy** (`deploy_model` route). **Off the loop (AC2):** all blocking boto3 (`get_object`+body read, `upload_fileobj`, `head_object`, `delete_object`) and `joblib.load` now run via `asyncio.to_thread`; the sync `download_file_from_s3` is wrapped in `to_thread` at its three awaited-less async call sites (`eda_summary.py`, `feature_engineering.py`, `transformation_engine/data_utils.py`). **Concurrency (cross-family codex review, 2 rounds):** the cache shares **one** estimator object across concurrent requests, and serving runs `predict`/`predict_proba` via `to_thread` — sklearn predict is thread-safe but LightGBM/older-XGBoost boosters are **not**, so concurrent threads could race inside the shared booster. Round-1 fix (a per-model lock only in `production.py`) was **incomplete** — round-2 review caught that the internal `/ml/{id}/predict` route and batch paths call predict on the same shared object on the loop thread, racing an overlapping production `to_thread` predict. Final fix centralizes **`run_locked_inference(model_id, user_id, func)`** (PEP-695 generic; `to_thread` + a per-model lock from `get_inference_lock`) and routes **every** inference site through it — production, internal predict, batch (vectorized + sequential fallback) — closing the race **and** moving the internal/batch predicts off the loop (the AC's "wrap inference in `to_thread`"). The lock is held only inside the worker thread around the sync call, never across an `await` (no deadlock). **Not a bug (verified):** cache-hit staleness across workers — model artifacts are immutable (fresh id per train, no in-place overwrite), and the serving routes re-read the deploy flag + existence from Mongo before predict (a deleted model 404s regardless of another worker's cache); bounded by TTL. Tests: `tests/test_services/test_model_storage_cache.py` (TTL expiry, LRU eviction, per-user keying, disabled-when-nonpositive, `load_model` dedups S3+Mongo on a hit, invalidation reloads, per-model lock identity). **Beta limitations:** cache/locks are process-local (Redis is the multi-worker upgrade path); `last_used_at` is stamped only on a cache **miss** (the production path also updates it per-prediction via the monitoring service); cold-cache thundering-herd double-downloads (harmless, same bytes); inference locks are tiny + never evicted. **Out of scope:** the `key_hash` index (deferred to a separate issue per the AC); pandas `read_*` after the downloader still runs on the loop in the EDA/feature handlers (AC names only the downloader + inference).
- **Issue #266 (P1.3 security — insecure deserialization of model artifacts via joblib/pickle):** `joblib.load` (`model_storage.py`) / `pickle.loads` (`redis_cache.py`) execute arbitrary code on deserialize, so a store tamper (the removed unauthenticated presigned-PUT #252/#291, or a Redis tamper) was an RCE primitive. Fix = **authenticate artifact bytes with HMAC-SHA256 before deserializing**. New stateless `app/utils/artifact_signing.py` (`sign_bytes`/`verify_bytes`; `hmac.compare_digest`; `verify_bytes` never raises — returns `False` on missing/non-ASCII sig; key resolves `ARTIFACT_SIGNING_KEY` → `NEXTAUTH_SECRET` → insecure dev fallback that only fires when neither is set, i.e. local dev, and logs a warning — so signing is secure by default in any real deploy). **AC2 (model artifacts):** `MLModel` += optional `model_signature`/`feature_transformer_signature` (None for pre-#266 models → degrade). **Signature stored in trusted Mongo, artifact in less-trusted S3** — a bucket tamperer can swap the pickle bytes but can neither forge a valid HMAC (no secret) nor alter the Mongo-stored signature. `save_model` signs the exact uploaded bytes; `load_model._verify_and_load` verifies before `joblib.load` and **raises on mismatch** (RCE refused); a **None** signature (legacy) loads with a warning (AC1 IAM backstops). The #265 artifact cache short-circuits verification on hits (already-loaded, trusted). **AC3 (Redis):** `_serialize_value` keeps simple values as plain JSON (JSON can't execute code) and wraps complex objects as a **signed pickle** (`\x00SPKL:` prefix + hex HMAC + payload); `_deserialize_value` verifies before `pickle.loads` and refuses unsigned/tampered blobs (raises → caller's `except` → cache **miss**; `get()` logs the expected `ValueError` at **WARNING** not ERROR so a first-deploy wave of legacy raw-pickle entries aging out isn't alarming). **AC1 (IAM bucket-write restriction):** cloud config, no IaC in-repo (like #259 AC3) → documented in new `docs/deployment/ARTIFACT_INTEGRITY.md` (threat model, HMAC design, a ready-to-apply Deny-non-backend-writes bucket policy, and a rotate-`NEXTAUTH_SECRET`-safely checklist step: set `ARTIFACT_SIGNING_KEY` to the current value first so auth rotation doesn't invalidate every signed model). `ARTIFACT_SIGNING_KEY` added to `.env.example` (optional; falls back to `NEXTAUTH_SECRET`). **Verified with a real-stack tamper demo** (Mongo + LocalStack): saved a signed model → overwrote its S3 object with a malicious `__reduce__` pickle → `load_model` **REFUSED** with a signature-mismatch `ValueError` and the RCE payload **never executed**; the real save→verify-load→predict integration roundtrip passes with signing active. **Out of scope:** `model_export.py:229/235` (`pickle.load`) lives inside a **generated client inference script** the user runs on their own machine — client-side code, not a backend deserialization sink. Tests: `test_utils/test_artifact_signing.py`, `test_services/test_model_storage_signing.py` (valid/tampered/legacy-warn+load), extended `test_services/test_redis_cache.py` (signed roundtrip, tampered + unsigned pickle refused); `sample_ml_model` mock fixture +2 fields (recurring `response_model=MLModel` gotcha). Cross-family review (opencode/GLM-5.2): "No blocking findings. Ship it." **Beta limitations:** legacy pre-#266 artifacts have no signature and load with a warning (can't be retro-signed → retrain to sign; IAM backstops); rotating the signing secret invalidates existing signatures (retrain); signing key + `get_signing_key` env read are per-call (fine at beta scale); cache/locks process-local (#265).
- **Issue #269 (P1.6 security — internal exception text leaked to clients across ~150 endpoints, incl. auth 500s):** ~150 handlers returned raw `str(e)` in 5xx bodies (leaking paths, S3 keys, model/feature contracts, stack detail) and auth echoed JWT-library internals + turned decode failures into 500s (polluting the #85 `error_rate`). **Central chokepoint fix, not a 150-file sweep** — FastAPI dispatches every `HTTPException`/unhandled exception through registered handlers *before* the body reaches the client, so one handler neutralizes all leak sites. New `app/middleware/error_handlers.py`: `RequestIDMiddleware` (added **last ⇒ outermost**; stamps `request.state.request_id` from an inbound `X-Request-ID` or a fresh `uuid4().hex`, echoes it on every response header) + `safe_http_exception_handler` + `unhandled_exception_handler` (registered via `register_error_handlers(app)` in `main.py`). **Every 5xx** body becomes a **status-appropriate generic** message (`_GENERIC_5XX_BY_STATUS`: 500→"Internal server error", 503→"Service temporarily unavailable", 502/504→…) **+ `request_id`** — `exc.detail` is **never echoed on 5xx**, so no `str(e)` can leak whatever a route raises (the handler still builds nothing from the exception; routes' now-discarded `f"…{str(e)}"` strings are logged server-side via the handler's `logger.error`/`logger.exception`). **4xx passes through unchanged** (safe validation text preserved; `exc.headers` copied so `WWW-Authenticate`/`Retry-After` survive, then `X-Request-ID` added). Handler registration needs `# type: ignore[arg-type]` (Starlette types handlers as `(Request, Exception)`; the narrower `HTTPException` sig is intentional). **Test gotcha:** an *unhandled* exception reaches `ServerErrorMiddleware` (outermost, above user middleware) which sends the 500 **and re-raises** for server logging — httpx `ASGITransport` surfaces that re-raise, so handler tests need `raise_app_exceptions=False` to mirror a real server. **Auth (`nextauth_auth.py`):** `JWTError` and the unexpected-`Exception` path now return a **fixed 401** `"Invalid authentication token"` (was `f"Invalid token: {str(e)}"` / a leaky **500**); real detail still `logger.error`'d server-side; the missing-`NEXTAUTH_SECRET` config error stays a genuine 500. **`health.py` `/health/ready`** (unauthenticated, returns a `JSONResponse` **directly** ⇒ bypasses the handler): per-dependency `"error"` is now generic `"unavailable"` (both the `except → str(e)` branches and the `asyncio.gather(return_exceptions=True)` fallback via new `_check_or_generic`), raw text (may contain Mongo/S3 connection strings/creds) logged server-side only. **Frontend** `app/api/chat/route.ts` returns a generic message instead of `error.message` (upload/auth/preview routes were already generic-500). **AC decision — 4xx `str(e)` is intentionally preserved** ("keep specific safe 4xx validation text only"): the ~94 `ValueError → 400, detail=str(e)` sites are the intended-safe category, untouched. Tests: new `tests/test_middleware/test_error_handlers.py` (5xx generic+request_id+no-leak, status-appropriate 503, 4xx passthrough, `X-Request-ID` echo, real-app registration); updated the tests that **asserted the leak** (`test_data_processing`/`test_ai_analysis`/`test_model_export`/`test_cache`/`test_health_checks` + `test_nextauth`) into **no-leak regression** assertions; added a `/health/ready` no-leak test. **Cross-family review (`codex`) caught 2 MAJOR** before PR — the `/health/ready` 503 leak (returned-JSONResponse bypasses the handler) and non-500 5xx passthrough (echoing `exc.detail` left the class open) — both fixed (health generic + never-echo-detail-on-any-5xx). **Beta limitations:** 4xx `str(e)` preserved per AC (targeted follow-up if a specific 4xx is later found to leak, e.g. an S3 `ValueError`); the ~73 route sites still build discarded `f"…{str(e)}"` strings (harmless, not refactored); request-id is per-request (not sourced from an upstream trace/APM system).
- **Issue #270 (P1.7 security — unbounded upload read → memory DoS; unscanned CVEs):** every upload route did `content = await file.read()` with **no cap**, so a large/crafted upload exhausted memory before any size check. **Defense-in-depth, cheapest-first:** (1) nginx `client_max_body_size 100M` (edge, already present); (2) new `app/middleware/body_size_limit.py::BodySizeLimitMiddleware` (added before CORS ⇒ inner, CORS decorates the 413) rejects on **`Content-Length` before the multipart parser spools** the body — standard clients (browsers, httpx/requests) always send it; (3) new shared `app/utils/upload_limits.py::read_upload_capped()` (413 at `MAX_UPLOAD_BYTES`, default 100 MB, env-overridable) streams in 1 MB chunks so the in-route RAM read stays bounded, wired into `upload.py`/`secure_upload.py` (×3)/`datasets.py`/`batch_prediction.py`. Added `except HTTPException: raise` before the catch-all in `upload.py`+`batch_prediction.py` so the 413 (and pre-existing 400s) aren't masked as 500 (`datasets.py` already had it). **Chunked completion** (`secure_upload.py`) also loaded the whole assembled file (chunked sessions allow very large files) into RAM via `f.read()` → now 413s over the cap + cleans up the temp file first (codex round-2 catch). **CVE bumps:** `starlette>=1.3.1` (CVE-2025-54121 multipart event-loop block **+** later Host-header auth-bypass PYSEC-2026-161/248 — relevant here: `rate_limit.py` gates on `request.url.path` — urlencoded-form DoS PYSEC-2026-249, FileResponse ReDoS PYSEC-2026-1942; pulls fastapi 0.139), `python-multipart>=0.0.31` (parser DoS CVEs), `urllib3>=2.5.0` (CVE-2025-50181/50182), `requests>=2.33.0` (CVE-2026-25645); frontend `npm audit fix` → 0 vulns. **Framework-upgrade side effect:** `HTTPBearer` now returns **401 (not 403) for missing credentials** (correct semantics) — two stale `async_test_client` assertions updated (`test_datasets`/`test_monitoring`); foreign-resource-access 403s unchanged. **CI (AC5):** new **advisory** `security-audit` job (pip-audit + `npm audit --audit-level=high`), **excluded from the required `CI Success` aggregate** (dep scanning is non-idempotent — new advisories land over time); `.github/dependabot.yml` gains **pip** (`/apps/backend`) + **npm** (`/apps/frontend`) ecosystems (grouped minor/patch). Tests: `test_utils/test_upload_limits.py`, `test_middleware/test_body_size_limit.py`, `test_api/test_upload.py` (route + e2e multipart 413 + middleware Content-Length 413), `test_api/test_secure_upload.py::TestChunkedCompletionSizeCap`. **Cross-family `codex` (2 rounds):** R1 flagged the in-route cap runs after parse/spool → added the middleware; R2 confirmed + flagged the chunked-completion `f.read()` → capped it. **Beta limitations:** a chunked body with **no** `Content-Length` still reaches the parser (bounded by nginx edge + in-route RAM cap; full streamed-abort skipped — fragile ASGI exception-flow, nginx covers it); advisory pip-audit surfaces a pre-existing transitive long tail (pillow/lxml/pyarrow/pytest majors) left for Dependabot; `fastapi>=0.116.1` resolves to 0.139 (validated by the full suite).

### MCP Server
- Located in `apps/mcp/`
- FastMCP framework
- Tools for advanced data processing and modeling
- **Issue #254 (P0.4 security — MCP unauthenticated on 0.0.0.0 + arbitrary S3 download → cross-tenant exfiltration):** the SSE server ran `host="0.0.0.0"` with no auth and `eda_summary` downloaded any caller-supplied `file_uri` with the server's AWS creds and **no owner check** (confused deputy → account-wide, cross-tenant S3 read). Fix (all `apps/mcp/`): `main.py` binds **`127.0.0.1`** by default (`MCP_HOST`), requires **`MCP_API_KEY`** bearer token via a **pure-ASGI** `BearerAuthMiddleware` (`auth.py`; SSE-safe — not `BaseHTTPMiddleware`, which buffers/breaks the stream; `hmac.compare_digest`), and **fails closed** (`require_api_key`) when the key is unset. `EdaInput` now takes **`dataset_id` + `user_id`** (both `min_length=1`); `run_eda_summary` resolves `UserData` (`utils/user_data.py::get_user_data_by_id`, lazy beanie init, returns `None` on bad id), verifies ownership via pure `authorize_dataset(ud, user_id)->s3_url`, and derives the S3 key **server-side** from the stored record — callers never supply a bucket/key/URL. `utils/s3_service.download_dataset_file` pins downloads to the app bucket (`AWS_S3_BUCKET`/`AWS_BUCKET_NAME`) via `parse_and_validate_s3_url` (accepts `s3://b/k` and `https://b.s3[.region].amazonaws.com/k`, rejects other buckets/unrecognized URLs). **Generic errors** everywhere: missing vs. unauthorized vs. S3-failure are indistinguishable (no `AccessDenied`/`NotFound` leak, no enumeration). Restored the empty `UserData` model (lean read-only mirror — user_id/s3_url/file_path; undeclared fields ignored on parse) and added `beanie`/`motor`/`starlette`/`uvicorn` deps the ownership path needs. Tests: `tests/test_auth.py`, `tests/test_utils/test_s3_service.py`, rewritten `tests/test_tools/test_eda_summary.py` (ownership allow/deny, generic errors, `pytest-asyncio` auto mode) — 28 passed. **Gated behind MCP non-startability (separate issue):** vendored FastMCP isn't pip-installed so `import fastmcp` fails at startup (pre-existing, unchanged) — these controls take effect once the server can start; "must be fixed before any MCP deploy." **Follow-up:** the backend `MCPIntegrationService` still POSTs `{schema,statistics,quality_report}` to `/tools/{name}` (a path the SSE server doesn't expose → already 404s/falls back today); wiring it to the new `dataset_id`/`user_id` contract + the MCP/SSE protocol belongs with the startability issue. **Beta trust boundary:** the bearer token is a single shared secret, so MCP trusts the (localhost, authenticated) backend to assert the correct `user_id`; per-user MCP tokens are future hardening.
- **Issue #255 (P0.5 — MCP server cannot start; single tool incompatible with real datasets):** unblocks #254's controls, which were **gated behind non-startability**. Root cause: `main.py` put the **vendored** `fastmcp/src` on `sys.path` and imported a FastMCP that calls `importlib.metadata.version('fastmcp')` — nothing pip-installs `fastmcp`, so startup raised `PackageNotFoundError` → `sys.exit(1)`. Fix: import `FastMCP` from the installed **`mcp.server.fastmcp`** (the `mcp` SDK, floor bumped `>=1.6.0`→`>=1.25.0`; `.tool()`/`.sse_app()` are drop-in), **delete the vendored `fastmcp/`** tree (143 files) and the `sys.path` hack. AC2 (reuse the backend's `parse_s3_url`) was already satisfied by #254's `utils/s3_service.parse_and_validate_s3_url` — a **superset** (adds bucket allowlisting); kept MCP-local since the backend isn't a dependency of the MCP app. AC3 (temp cleanup in `finally`) also already done in #254. **AC4:** `run_eda_summary` now offloads the blocking S3 download + `pd.read_csv` + pandas analysis via `asyncio.to_thread` (extracted the sync body into `_build_summary`) so the async SSE handler never stalls the loop; temp cleanup stays in the async `finally`. **Deslop:** removed dead `tool_runner.py` (called a nonexistent `eda_summary.run`, imported nowhere) and the empty `tools/model_train.py`/`tools/null_analysis.py` stubs; fixed `render.yaml` (Poetry + `pip install -e ./fastmcp` → `uv sync` / `uv run python main.py`). **Docs (AC6):** rewrote `README.md` to reality (single `eda_summary_tool`, `main.py` entry point — not the fictional `server.py`, actual dir layout, real run command; dropped the ~9 advertised-but-nonexistent tools and the vendored-fastmcp link) and corrected this file's MCP-setup snippet. Tests (AC5): non-mocked real-CSV read + cleanup-on-read-failure in `test_tools/test_eda_summary.py`, subprocess `import main` startup smoke in `tests/test_main_startup.py` (run with cwd=`apps/mcp` so `mcp` resolves to the SDK, not the local app shadowed by pytest's `pythonpath=..`); existing non-mocked URL-parse tests unchanged — 36 passed. **Review fixes (codex/claude-review):** cleanup moved *inside* the worker thread (`_download_and_summarize` owns download+build+`os.remove` in one `finally`) — `asyncio.to_thread` can't cancel the thread, so a split across the `await` boundary would leak the dataset on request cancellation; `download_dataset_file` also removes its own temp file on a failed download; `calculate_variable_insights` **stringifies correlated-pair keys** (`"a & b"`) because `.unstack().to_dict()` yields tuple keys that break the FastMCP JSON response for any dataset with a >0.8-correlated feature pair (verified live); renamed the `utils/__iniy__.py` typo → `__init__.py`. **Beta limitation / follow-up (unchanged from #254):** the backend `MCPIntegrationService` still POSTs `{schema,...}` to `/tools/{name}` (a path the SSE server doesn't expose → 404s/falls back); wiring it to the `dataset_id`/`user_id` SSE contract is a separate integration issue.

## Testing Commands
- Backend (full suite): `cd apps/backend && uv run pytest` — requires MongoDB on localhost:27017; optional test Redis (6380) and LocalStack (4566) via `docker compose -f docker-compose.test.yml up -d` (tests skip with a reason when these are absent)
- Backend (service-free tests, no services): `cd apps/backend && PYTHONPATH=. uv run pytest tests/test_security/ tests/test_processing/ tests/test_utils/ tests/test_models/ tests/test_auth/ tests/test_model_training/test_problem_detector.py tests/test_model_training/test_feature_engineer.py -m "not integration and not performance" -v`
- Frontend (unit tests): `cd apps/frontend && npm test`
- Frontend (type check): `cd apps/frontend && npm run type-check`
- MCP: `cd apps/mcp && uv run pytest tests/` (scope to `tests/`; the vendored `fastmcp/tests/` is the upstream library's own suite)
- **Route-test footgun (issue #267):** `tests/test_api/conftest.py::mock_async_client` builds a *stub* FastAPI app mounting **only** `health` + `secure_upload`, so any request to another router (`/api/v1/production/*`, `/api/v1/monitoring/*`, `/ml/*`, …) 404s unconditionally — assertions like `in [200, 404, 422]` then pass vacuously. For real route tests use **`async_authorized_client`** (drives the full `app.main.app` with the auth dependency overridden) + real Mongo docs and assert **exact** statuses/bodies. For the X-API-Key production-serving surface, insert a real `APIKey` and pass the raw `sk_live_…` header (that surface uses `verify_api_key`, not the session override). Monitoring metrics read a **process-global in-memory** `prediction_log` that `setup_database` (Mongo-only) never clears — reset it (autouse `prediction_log.logs.clear()`) when asserting per-model counts.
- **Frontend jest.setup contract (issue #268):** the global `apps/frontend/jest.setup.js` `beforeEach` sets `global.fetch` to **REJECT** by default (`"Unmocked fetch call …"`) — a component/page test that triggers a fetch **must** stub `global.fetch` explicitly (the old silent `{ ok:true, json:()=>({}) }` default hid contract regressions). `next/navigation`'s `useRouter()` returns a **stable, per-test-cleared** spy object exposed as `global.__NEXT_ROUTER_MOCKS__` (`push`/`replace`/`prefetch`/`back`), so a test that doesn't re-mock `next/navigation` locally can assert redirects via `global.__NEXT_ROUTER_MOCKS__.push`. The contract is locked by `__tests__/setup/jestSetupContract.test.tsx`. Also from #268: `canAccessStage` has direct provider-level tests (`WorkflowContext.access.test.tsx`) instead of being mocked to `true`, the predict page and the `explore/[id]` graceful-degradation paths are covered (Radix tabs need the `mouseDown`+`mouseUp`+`click` sequence, not a bare `click`), and `test_preview_integration.py` asserts **exact** statuses (400 empty steps / 422 invalid type / 404 unknown dataset / 200 seeded dataset + mocked S3) — note the preview route applies only `transformation_steps[0]`.

## Test Suite Status
- Backend: full suite green locally (~1,460 passed, ~39 service-gated/documented skips) ✅ — fixed in issue #160
  - During pytest runs the app lifespan uses the **test** database (never production `MONGODB_URI`)
  - Canonical Beanie model registry: `app/models/registry.py` (shared by app lifespan and `setup_database` fixture)
- CI: `ci.yml` is the primary PR gate (issue #150). It runs backend (ruff **blocking** — default `E`/`F` plus `I` (isort) and `UP` (pyupgrade; PEP 585/604 annotations, `datetime.UTC`, etc. — issue #220), mypy **blocking** — a plain `uv run mypy app/` that fails on any error; the pre-existing-error baseline was burned down to zero and removed (no more `apps/backend/mypy-baseline.txt`, #226); issue #219, service-free pytest), frontend (eslint, `tsc --noEmit`, `next build`, jest), MCP pytest, and backend integration tests (MongoDB **service container** on 27017 + Redis/LocalStack from `docker-compose.test.yml`). The integration job sets **`CI_REQUIRE_SERVICES=true`** (issue #221): all three services are **hard requirements** — LocalStack must become healthy or the step `exit 1`s, and `require_service()` (`tests/conftest.py`) **fails** (not skips) on a missing service, so the S3/upload (`test_upload_workflow.py` runnable tests)/OpenAI (`test_openai_fixtures.py`, fully mocked) integration suites run in the gate and can never silently skip. `setup_database` cleans at **start** as well as teardown (run-order-independent isolation), and the `async_*_client` `LifespanManager`s use 30s startup/shutdown timeouts to avoid CI-contention flakes. The functional E2E **`@smoke`** gate runs as the `e2e-smoke` job inside `ci.yml` (folded in from the former standalone `smoke-tests.yml`, issue #210, after #157 stabilized it and a 10+ PR green soak) — Playwright/`chromium-smoke` against the full stack (Mongo service container + manually-started MinIO S3). A single aggregate `CI Success` status is the required check for branch protection on `main`, and it now **depends on `e2e-smoke`** — so a red smoke run blocks merge (the required-check set is unchanged: still just `CI Success`, which now transitively enforces backend lint/unit/integration, frontend lint/typecheck-build/unit, MCP, and E2E smoke; advisory-only `backend-typecheck` and the non-blocking `@perf` job in `perf-tests.yml` are deliberately excluded). The old `unit-tests.yml` was consolidated into `ci.yml`; `integration-tests.yml`/`e2e-tests.yml` remain manual (`workflow_dispatch`). `deploy.yml` deploys `main` to staging over SSH (secret-gated; see workflow header) — service requirements documented in each workflow header. `claude-code-review.yml` posts an **advisory** Claude review on each PR — hardened after the PR #187 runaway incident with a 10-min job timeout, `--max-turns 20` via `claude_args` (the action has no `max_turns` input), a per-PR concurrency group with cancel-in-progress, and a prompt that mandates a single consolidated comment (issue #188); `claude.yml` answers `@claude` mentions (30-min timeout, `--max-turns 50`)
- Frontend: Jest tests configured; TypeScript errors eliminated (#166); `npm run type-check` (`tsc --noEmit`) enforced in CI
- MCP: Pytest suite available
- See `apps/backend/docs/TEST_INFRASTRUCTURE.md` for the testing guide and the **Service Prerequisites** table
- See `apps/backend/docs/TDD_GUIDE.md` for TDD methodology

## Environment Variables
- Frontend: `.env.local`
- Backend: `.env`
- Required: AWS credentials, MongoDB URI, OpenAI API key, NextAuth secret
- Development: Set `SKIP_AUTH=true` to bypass authentication — only honored when `ENVIRONMENT` is explicitly `development`/`test`; backend startup fails hard otherwise (#149)

## Data Flow
1. User uploads file → Backend processes → Stores in S3
2. Metadata saved to MongoDB
3. Background AI analysis triggered
4. Frontend displays results with visualizations

## Current Stage
**Sprint 11 Complete** ✅ | **Sprint 12 Ready** 🟢

See `apps/backend/docs/SPRINTS.md` for detailed sprint history.

## MCP Server Setup
This project includes a custom MCP server (`apps/mcp/`) exposing the `eda_summary_tool` over an SSE transport. It is intended to be called by the backend on the same host, not wired into Claude Desktop. The entry point is `main.py` (there is no `server.py`); FastMCP comes from the installed `mcp` SDK (`mcp.server.fastmcp`), not a vendored package (issue #255).

```bash
cd apps/mcp
uv sync
MCP_API_KEY=<secret> uv run python main.py   # binds 127.0.0.1; fails closed without MCP_API_KEY
```

See `apps/mcp/README.md` for the full tool list, required env, and the security model.

Additional recommended MCP servers:
- **Context7** - For library documentation lookup
- **Serena** - For project memory and session management

## Documentation Requirements

**ALL implementation documentation MUST remain synchronized with the codebase**:

1. **API Documentation**:
   - Update OpenAPI specifications when endpoints change
   - Document all request/response schemas
   - Include example requests and responses
   - Document error responses and status codes

2. **Code Documentation**:
   - Python: Docstrings for all public functions, classes, and modules
   - TypeScript: JSDoc comments for complex functions and components
   - Update inline comments when implementation changes
   - Remove outdated comments immediately

3. **Implementation Documentation**:
   - Update relevant sections in this CLAUDE.md file
   - Keep architecture diagrams current
   - Update configuration examples when defaults change
   - Document breaking changes prominently

4. **README Updates**:
   - Keep feature lists current
   - Update setup instructions when dependencies change
   - Maintain accurate command examples
   - Update version compatibility information

5. **CLAUDE.md Maintenance**:
   - Add new patterns to relevant sections
   - Update "Current Stage" when workflow changes
   - Keep command examples accurate and tested
   - Document new testing patterns or quality gates

## Automated Workflow Configuration

### Traycer AI Integration

When receiving Traycer AI prompts:
1. Save prompt to: `prompts/<issue-id>.txt`
2. Run: `./scripts/traycer-workflow.sh <issue-id>`
3. Monitor: `npx claude-flow@alpha status --watch`
4. Only intervene if blocker queue triggered

### Quality Gates

Before PR creation:
- ✅ All tests must pass (100% requirement)
- ✅ Test coverage >85%
- ✅ Linting (ruff/eslint) passes
- ✅ Type checking (mypy/tsc) passes
- ✅ No TODO/FIXME/NotImplemented markers
- ✅ Security scan (OWASP patterns)

### CodeRabbit Configuration

Max iterations: 3
Auto-fix categories:
- Code style and formatting
- Type errors
- Simple logic bugs
- Documentation improvements

Blocker queue triggers:
- Iteration 3 still has failing tests
- Architecture change suggestions
- Security vulnerabilities requiring human decision
